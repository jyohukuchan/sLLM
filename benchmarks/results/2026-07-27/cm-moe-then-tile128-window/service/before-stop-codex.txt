2202045 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad cat > $S/codex-CM-window.md <<'"'"'EOF'"'"' # 依頼CM: ハーネスを窓なしで検証してから、1 窓で MoE → tile-128 品質を片付ける  ## 経緯: 同じ不具合で GPU 窓を 2 回失っている  - **依頼CK**: numeric capture の出力先を**事前作成**していて fail-fast。品質判定に到達せず。窓 1 回消費。 - **依頼CL**: その修正が不完全（runner が新規作成するのは `numeric/<route>` 自体なのに、   親も消して `numeric/<route>/oracle` を渡した）。**GPU 初期化前に fail-fast。**窓 1 回消費。   しかも fail-fast がスクリプトごと落としたため、**同じ窓で予定していた MoE にも到達しなかった。** - 現在は `numeric/<route>` を未作成で渡すよう修正済み（コミット `62e2ff49`）。**未検証である。**  **MoE の実機実行は CE / CH / CI / CL の 4 セッション連続で到達していない。**  ## 本タスクの構成（この順を守ること）  ### Phase 1: ハーネスを GPU 窓なしで検証する（最重要）  **失敗箇所はディレクトリ生成という「モデル非依存の配管」である。** **稼働中の本番ゲートウェイに対して同じハーネスを通せば、窓ゼロで配管を全部踏める。**  1. **本番サービスを止めずに**、現在稼働中の gateway に対して    `tools/lightweight_promotion.py` の capture → compare → markdown 出力までを    **端から端まで 1 回通すこと。**    本番は `AQ4_0` Qwen3.5-9B（manifest    `a654d92fe8142fcc0904fe187c96b84c95e0dd18acac61ef25d0cfa6429a08cd`）で `active` である。    **読み取り専用のリクエストだけなので停止は不要である。** 2. **ケース数を絞ってよい**（1〜2 ケースで十分）。目的は配管の確認であって品質判定ではない。 3. **ディレクトリ生成・引数渡し・比較・markdown 出力が全部通ることを確認すること。**    ここで落ちるなら直すこと。**通るまで窓を取らないこと。** 4. **この Phase は本番に一切影響を与えてはならない。**    `active.json` を変更しない、サービスを停止しない、`/opt/ullm` を変更しない。  ### Phase 2: 窓を 1 回取り、MoE を**先に**実行する  **MoE はハーネスに依存しない。tile-128 の失敗に巻き込まれる理由がない。だから先にやる。**  依頼CH が契約を修正済み（`217992c4` / `190465e4`）。MoE binary は最新 release を確認済み （`6ee827…e39cd`）。**実行前に改めてビルドが最新であることを確認すること** （依頼CI は stale binary で失敗した）。  1. **VRAM 実測** — ledger `30,858,010,436 B`（262,144-token / F16 KV）が確保できるか。    できないなら**何が超過したかを数字で**。文脈長を縮めれば載るなら縮めてよい（明記すること）。 2. **実際の生成文**（短くてよい、遅くてよい） 3. **全 40 層の route 照合**（runtime route 対 raw BF16 router の独立再計算）。    **MoE は routing を間違えてもそれらしい文章が出る。生成文だけで正しさを判断しないこと。** 4. **prefill / decode の速度**  package: `/home/homelab1/datapool/ullm/product/qwen35-35b-a3b-aq4_0-g8-moe-v0.2/`。 **MoE の本番昇格は行わない。**  ### Phase 3: 同じ窓で tile-128 の品質判定  **Phase 2 が失敗しても Phase 3 を実行すること。逆も同じ。** **各 Phase を独立したステップとして実行し、片方の失敗が他方を殺さない構造にすること。** `set -e` 相当でスクリプト全体が落ちる作りにしないこと。  依頼CK が速度を測定済み（**再測不要**）:  | 構成 | full-model decode tok/s | |---|---:| | direct | 15.356923 | | **grouped tile 128** | **26.311639**（tile-20 比 95.46%、direct 比 1.714×） | | grouped tile 20（同窓） | 27.561622 |  **依頼CF と同一条件で grouped tile-128 対 direct の生成文を比較すること。**  - **同じ 8 ケース**（コード生成 2 件、日本語 multi-turn 1 件を含む）、**同じトークン上限 512** - **`javascript_debug_extended` を必ず含めること。** tile-20 はここで   「元の `filter(Boolean)` は `NaN` を含めない」という**事実誤り**を出し、   **direct は同じ上限で出していない。** - CF の証跡（direct 側）は   `/home/homelab1/coding-local/ultimateLLM/uLLM-sq8-production-switch-cf/benchmarks/results/2026-07-27/sq8-production-switch/`   にある。**読み取りのみ。**再利用してよい。 - 判定は `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従い、   **実際の生成文**で。**厳密な数値閾値を合否に使わないこと。** - **`split_vs_direct` の最大絶対差も記録すること。** tile-20 では `1.08033e-7`（約 1 ULP）。  **tile 128 でも品質が落ちるなら**、原因はタイル幅ではなく GQA 協調そのものになる。 その場合、**本番 `AQ4_0` が `aq4_gqa_grouped_split` / tile 128 で品質 10/10 を通っている事実と 矛盾する。** 矛盾するならその考察を書くこと。  **本番の差し替えは本タスクでは行わない。**  EOF cat >> $S/codex-CM-window.md <<'"'"'EOF'"'"' ## GPU 窓の作法（厳守）  **窓は 1 回だけ。Phase 1 を通してから取ること。**  1. 取る前に確認:    ```    fuser -v /run/ullm/r9700.lock 2>&1    pgrep -af '"'"'codex exec'"'"'    systemctl show ullm-openai.service -p ActiveState -p NRestarts    ``` 2. **停止前に active manifest SHA-256 を記録すること。**    現在 `a654d92fe8142fcc0904fe187c96b84c95e0dd18acac61ef25d0cfa6429a08cd`    （worker `/opt/ullm/aq4-overnight-consolidation-v0.1/releases/aq4-consolidated-840a1c7a-5a274733/ullm-aq4-worker`、    `root:root` `0555`）。**`active.json` を変更しないこと。** 3. **service stop → lock 取得 → Phase 2 → Phase 3 → lock 解放 → service start** の順。    **依頼CF は「lock 解放前に start して `WorkerBusy` → start-limit」を踏んでいる。    必ず lock を解放してから start すること。** 4. `StartLimitBurst=3` / `StartLimitIntervalSec=900`。 5. 復旧後、**実際に応答することを確認すること**（ゲートウェイ準備に約 3,195 ms）。 6. **熱ゲートは到達可能な閾値にすること**（edge ≤ 45 ℃ で回せた実績。40 ℃ 固定は永久待ち）。    R9700 は junction crit 110 ℃。`amd-smi` の `THROTTLE_STATUS: THROTTLED` は    具体的 violation が全て N/A のことがあり、それ自体はクロック低下の証拠ではない。**実クロックを見ること。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。   **FP8 = S1E4M3、BF8 = S1E5M2。** - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止。** - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ## 並行実行  **現在ほかに GPU を使うセッションはない。** 本タスクが単独で窓を使う。  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は共有作業木で実行しない。  ## 成果物  - **Phase 1**: ハーネスが窓なしで端から端まで通った証跡 - **Phase 2**: MoE の VRAM 実測、生成文、40 層 route 照合、prefill / decode 速度 - **Phase 3**: tile-128 対 direct の生成文並置、品質判定、`split_vs_direct` 最大絶対差 - **本番の復旧確認**（manifest SHA-256、応答、`NRestarts`） - `journal/2026/07/27/` に house style で記録 - `Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` 付きのクリーンなコミット  ## 報告  (1) **Phase 1 は通ったか**（通らなければ何を直したか）、 (2) **MoE: VRAM は載ったか、生成文は出たか、40 層 route 照合は通ったか、速度は**、 (3) **tile 128 の品質は通ったか**（`javascript_debug_extended` の生成文を具体的に）、 (4) `split_vs_direct` 最大絶対差の tile 依存、 (5) tile 128 でも落ちた場合、`AQ4_0` が同じ構成で通っている事実との整合、 (6) **本番の復旧**（manifest SHA-256、`NRestarts`）、(7) 窓の回数、(8) コミットハッシュ。 EOF codex exec -m gpt-5.6-terra -c model_reasoning_effort="high" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-CM-window.md)" < /dev/null 2>&1 | tail -20' && pwd -P >| /tmp/claude-7903-cwd
2202049 codex exec -m gpt-5.6-terra -c model_reasoning_effort=high -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼CM: ハーネスを窓なしで検証してから、1 窓で MoE → tile-128 品質を片付ける  ## 経緯: 同じ不具合で GPU 窓を 2 回失っている  - **依頼CK**: numeric capture の出力先を**事前作成**していて fail-fast。品質判定に到達せず。窓 1 回消費。 - **依頼CL**: その修正が不完全（runner が新規作成するのは `numeric/<route>` 自体なのに、   親も消して `numeric/<route>/oracle` を渡した）。**GPU 初期化前に fail-fast。**窓 1 回消費。   しかも fail-fast がスクリプトごと落としたため、**同じ窓で予定していた MoE にも到達しなかった。** - 現在は `numeric/<route>` を未作成で渡すよう修正済み（コミット `62e2ff49`）。**未検証である。**  **MoE の実機実行は CE / CH / CI / CL の 4 セッション連続で到達していない。**  ## 本タスクの構成（この順を守ること）  ### Phase 1: ハーネスを GPU 窓なしで検証する（最重要）  **失敗箇所はディレクトリ生成という「モデル非依存の配管」である。** **稼働中の本番ゲートウェイに対して同じハーネスを通せば、窓ゼロで配管を全部踏める。**  1. **本番サービスを止めずに**、現在稼働中の gateway に対して    `tools/lightweight_promotion.py` の capture → compare → markdown 出力までを    **端から端まで 1 回通すこと。**    本番は `AQ4_0` Qwen3.5-9B（manifest    `a654d92fe8142fcc0904fe187c96b84c95e0dd18acac61ef25d0cfa6429a08cd`）で `active` である。    **読み取り専用のリクエストだけなので停止は不要である。** 2. **ケース数を絞ってよい**（1〜2 ケースで十分）。目的は配管の確認であって品質判定ではない。 3. **ディレクトリ生成・引数渡し・比較・markdown 出力が全部通ることを確認すること。**    ここで落ちるなら直すこと。**通るまで窓を取らないこと。** 4. **この Phase は本番に一切影響を与えてはならない。**    `active.json` を変更しない、サービスを停止しない、`/opt/ullm` を変更しない。  ### Phase 2: 窓を 1 回取り、MoE を**先に**実行する  **MoE はハーネスに依存しない。tile-128 の失敗に巻き込まれる理由がない。だから先にやる。**  依頼CH が契約を修正済み（`217992c4` / `190465e4`）。MoE binary は最新 release を確認済み （`6ee827…e39cd`）。**実行前に改めてビルドが最新であることを確認すること** （依頼CI は stale binary で失敗した）。  1. **VRAM 実測** — ledger `30,858,010,436 B`（262,144-token / F16 KV）が確保できるか。    できないなら**何が超過したかを数字で**。文脈長を縮めれば載るなら縮めてよい（明記すること）。 2. **実際の生成文**（短くてよい、遅くてよい） 3. **全 40 層の route 照合**（runtime route 対 raw BF16 router の独立再計算）。    **MoE は routing を間違えてもそれらしい文章が出る。生成文だけで正しさを判断しないこと。** 4. **prefill / decode の速度**  package: `/home/homelab1/datapool/ullm/product/qwen35-35b-a3b-aq4_0-g8-moe-v0.2/`。 **MoE の本番昇格は行わない。**  ### Phase 3: 同じ窓で tile-128 の品質判定  **Phase 2 が失敗しても Phase 3 を実行すること。逆も同じ。** **各 Phase を独立したステップとして実行し、片方の失敗が他方を殺さない構造にすること。** `set -e` 相当でスクリプト全体が落ちる作りにしないこと。  依頼CK が速度を測定済み（**再測不要**）:  | 構成 | full-model decode tok/s | |---|---:| | direct | 15.356923 | | **grouped tile 128** | **26.311639**（tile-20 比 95.46%、direct 比 1.714×） | | grouped tile 20（同窓） | 27.561622 |  **依頼CF と同一条件で grouped tile-128 対 direct の生成文を比較すること。**  - **同じ 8 ケース**（コード生成 2 件、日本語 multi-turn 1 件を含む）、**同じトークン上限 512** - **`javascript_debug_extended` を必ず含めること。** tile-20 はここで   「元の `filter(Boolean)` は `NaN` を含めない」という**事実誤り**を出し、   **direct は同じ上限で出していない。** - CF の証跡（direct 側）は   `/home/homelab1/coding-local/ultimateLLM/uLLM-sq8-production-switch-cf/benchmarks/results/2026-07-27/sq8-production-switch/`   にある。**読み取りのみ。**再利用してよい。 - 判定は `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従い、   **実際の生成文**で。**厳密な数値閾値を合否に使わないこと。** - **`split_vs_direct` の最大絶対差も記録すること。** tile-20 では `1.08033e-7`（約 1 ULP）。  **tile 128 でも品質が落ちるなら**、原因はタイル幅ではなく GQA 協調そのものになる。 その場合、**本番 `AQ4_0` が `aq4_gqa_grouped_split` / tile 128 で品質 10/10 を通っている事実と 矛盾する。** 矛盾するならその考察を書くこと。  **本番の差し替えは本タスクでは行わない。**  ## GPU 窓の作法（厳守）  **窓は 1 回だけ。Phase 1 を通してから取ること。**  1. 取る前に確認:    ```    fuser -v /run/ullm/r9700.lock 2>&1    pgrep -af 'codex exec'    systemctl show ullm-openai.service -p ActiveState -p NRestarts    ``` 2. **停止前に active manifest SHA-256 を記録すること。**    現在 `a654d92fe8142fcc0904fe187c96b84c95e0dd18acac61ef25d0cfa6429a08cd`    （worker `/opt/ullm/aq4-overnight-consolidation-v0.1/releases/aq4-consolidated-840a1c7a-5a274733/ullm-aq4-worker`、    `root:root` `0555`）。**`active.json` を変更しないこと。** 3. **service stop → lock 取得 → Phase 2 → Phase 3 → lock 解放 → service start** の順。    **依頼CF は「lock 解放前に start して `WorkerBusy` → start-limit」を踏んでいる。    必ず lock を解放してから start すること。** 4. `StartLimitBurst=3` / `StartLimitIntervalSec=900`。 5. 復旧後、**実際に応答することを確認すること**（ゲートウェイ準備に約 3,195 ms）。 6. **熱ゲートは到達可能な閾値にすること**（edge ≤ 45 ℃ で回せた実績。40 ℃ 固定は永久待ち）。    R9700 は junction crit 110 ℃。`amd-smi` の `THROTTLE_STATUS: THROTTLED` は    具体的 violation が全て N/A のことがあり、それ自体はクロック低下の証拠ではない。**実クロックを見ること。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。   **FP8 = S1E4M3、BF8 = S1E5M2。** - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止。** - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ## 並行実行  **現在ほかに GPU を使うセッションはない。** 本タスクが単独で窓を使う。  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は共有作業木で実行しない。  ## 成果物  - **Phase 1**: ハーネスが窓なしで端から端まで通った証跡 - **Phase 2**: MoE の VRAM 実測、生成文、40 層 route 照合、prefill / decode 速度 - **Phase 3**: tile-128 対 direct の生成文並置、品質判定、`split_vs_direct` 最大絶対差 - **本番の復旧確認**（manifest SHA-256、応答、`NRestarts`） - `journal/2026/07/27/` に house style で記録 - `Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` 付きのクリーンなコミット  ## 報告  (1) **Phase 1 は通ったか**（通らなければ何を直したか）、 (2) **MoE: VRAM は載ったか、生成文は出たか、40 層 route 照合は通ったか、速度は**、 (3) **tile 128 の品質は通ったか**（`javascript_debug_extended` の生成文を具体的に）、 (4) `split_vs_direct` 最大絶対差の tile 依存、 (5) tile 128 でも落ちた場合、`AQ4_0` が同じ構成で通っている事実との整合、 (6) **本番の復旧**（manifest SHA-256、`NRestarts`）、(7) 窓の回数、(8) コミットハッシュ。
