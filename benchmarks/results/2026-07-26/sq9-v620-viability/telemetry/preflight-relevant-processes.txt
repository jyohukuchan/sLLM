  75072 homelab1 ullm-openai-gat /home/homelab1/coding-local/ultimateLLM/uLLM-project/services/openai-gateway/.venv/bin/python3 /home/homelab1/coding-local/ultimateLLM/uLLM-project/services/openai-gateway/.venv/bin/ullm-openai-gateway
  75562 homelab1 ullm-aq4-worker /home/homelab1/coding-local/ultimateLLM/uLLM-aq4-fidelity-promotion-release-f1a3cf4c/ullm-aq4-worker --served-model-manifest /etc/ullm/served-models/active.json
 678656 homelab1 codex           codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼F: SQ9_0 が V620 (gfx1030) で本当に価値を持つかの実機検証  ## 運用前提（全タスク共通）  - このマシン `homelab1-WRX80-Creator` は外部にサービス提供しておらず、本プロジェクトの開発専用である。   sudo（パスワード `Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）、GPU使用、   サービスの停止/起動は事前承認済みで、都度の確認は不要。 - ただし **final activation（`/etc/ullm/served-models/active.json` の実バイト差し替え）は   引き続き人間の明示承認が必要**。本タスクでは一切行わない。 - 命名規約: `AQ4` / `SQ8` / `SQ9` はフォーマットの**カテゴリ**名、   `AQ4_0` / `SQ8_0` / `SQ9_0` が**厳密なフォーマット**名。文書中は常に厳密名で書くこと。  ## 問い  **`SQ9_0`（FP9 / signed E5M3、スケールなし、9 bpp）は、FP8 演算器を持たない Radeon Pro V620 (gfx1030, RDNA2) 上で、`SQ8_0`（E4M3 + ブロックスケール、8 bpp）より速いか。**  直前の設計タスクで `docs/plans/sq9-format-design-input-v0.1.md`（コミット `5b55233d`）が確定しており、 そこでの採算条件は「`SQ8_0` フォールバック比で実効効率が約 7.29% 以上改善すること」である。 この閾値を実機で満たすかどうかを判定するのが本タスクの目的である。  ### なぜ机上で答えが出ないか（設計の勘所。必ず踏まえること）  - decode（M=1 GEMV）は**メモリ帯域律速**である。`SQ9_0` は 9 bpp なので、   読むバイト数の増加はそのまま不利に働く。 - `SQ9_0` が勝ちうるのは、**gfx1030 上での `SQ8_0` の dequant が実際に ALU 律速になっている場合のみ**である。   gfx1030 には FP8 変換命令がないため、E4M3 → FP16 は指数リバイアス（bias 7 → 15）、   非正規化数の特別処理、さらにブロックスケール乗算を要する。   これが何 VALU 命令に展開され、メモリ待ちに隠れるのか隠れないのかは、実測しないと分からない。 - したがって本タスクの核心は「帯域ペナルティ」対「ALU 削減」の**実測による勝敗判定**である。   どちらに転んでも正しい結果であり、`SQ9_0` が負けたならそれを明確に報告すること。   肯定的な結論を出すことが目的ではない。  ## 重要な実装上の注意  ### 1. デバイス選択（事故防止のため最優先）  **HIP のデバイス番号と `amd-smi` の GPU 番号は一致しない。** 実例として、 `llama-server --device ROCm0` は `amd-smi` の GPU2（R9700, `0000:47:00.0`）に載っていた。 したがって**インデックスでデバイスを選んではならない**。  `hipGetDeviceProperties` の `gcnArchName` を見て **`gfx1030` のデバイスを実行時に特定**すること。 搭載構成は Radeon Pro V620 ×2（gfx1030、`0000:03:00.0` と `0000:43:00.0`）と Radeon AI PRO R9700 ×1（gfx1201、`0000:47:00.0`）である。  **R9700 (gfx1201) では絶対に測定を実行しないこと。** そこには本番 AQ4_0 worker が常駐している。 測定開始前と終了後に、選んだデバイスの `gcnArchName` と PCI bus id をログに残すこと。  ### 2. フォーマットのレイアウトは実物を使うこと  - `SQ8_0` のブロック構成・スケールの型・スケール格納レイアウトは、**実際のコードから読み取って忠実に再現**すること   （`crates/ullm-engine/src/sq8_*.rs` と対応する HIP/C++ カーネル）。理想化した仮定で代用しない。   特に**スケール自体もバイトを消費する**点を正しく数えること。   128 要素あたり `SQ8_0` が 128 B + スケール、`SQ9_0` が 144 B + スケール 0 B なので、   実効的なバイト差は単純な 12.5% より小さくなるはずである。この実数を測定条件として明記すること。 - `SQ9_0` のレイアウトは `docs/plans/sq9-format-design-input-v0.1.md` の確定仕様に従うこと。   128 要素を「下位 8 bit plane 128 B」+「sign bit plane 16 B」に分離、計 144 B、16 B 整列、   変換は **9 bit コード全体の `<< 7` 一発**（マスク不要）。  ### 3. 正しさを先に確認すること  タイミングを測る前に、両フォーマットの dequant 出力が CPU 参照実装と一致することを GPU 上で確認すること。数値が合っていないカーネルの速度は無意味である。  ## 測定内容  1. **マイクロベンチ: dequant + GEMV（M=1、decode 相当）**    - 比較対象: `SQ9_0` / `SQ8_0` / 素の FP16（帯域上限の参照） / 可能なら `AQ4_0`（参考値）    - 実モデルの実形状を使うこと。対象モデルの重み行列形状をリポジトリから取得して用いる      （Qwen3-14B-FP8 系および Qwen3.5-9B 系の projection / MLP の実寸）    - 指標: 経過時間、達成帯域（GB/s）、V620 の理論帯域に対する効率、要素あたり実効コスト 2. **マイクロベンチ: GEMM（M を振る、prefill 相当）**    - M = 1, 8, 32, 128, 512 程度で掃引し、**帯域律速から演算律速へ移る境界**を特定すること。      `SQ9_0` の優劣がこの境界のどちら側にあるかが実用上の結論になる。 3. **dequant 単体コストの分離測定**    - dequant のみ（GEMV なし）を測り、`SQ8_0` の dequant が ALU 律速かどうかを直接示すこと。    - 併せてオフライン静的解析（VGPR/SGPR/LDS、ISA disassembly）で両者の      要素あたり VALU 命令数を数え、実測と突き合わせること。      これは本プロジェクトが P3 で確立した手法である。 4. **判定**    - 設計文書の採算条件（`SQ8_0` 比で実効効率 +7.29% 以上）を満たすかを明示的に判定すること。    - 満たさない場合、どの regime（M の範囲、行列形状）でも満たさないのか、      特定条件下では満たすのかを切り分けること。  ## 測定の作法  - V620 は 2 枚とも現在アイドル（busy 3% 未満）である。測定前に他プロセスが載っていないことを確認し、   記録すること。 - ウォームアップ、複数回試行、中央値/分散の報告を行うこと。単発測定を結論にしない。 - 測定中の GPU 温度・クロック・電力を記録すること   （`/sys/class/drm/card*/device/` 配下と `hwmon`）。V620 は現在 12〜16 W のアイドル状態である。 - **R9700 は触らない。** `llama-qwen35-udq4.service` は `inactive`/`disabled` のまま、   `ullm-openai.service` は `active` のまま維持すること。  ## 成果物  1. ベンチマークのソースとビルド手順を、本番ビルドツリーを汚さない場所に置くこと    （リポジトリ内なら `tools/` または `benchmarks/` 配下の適切な場所。既存の    `tools/bench-fused-lut-matmul-hip.cpp` が参考になる）。 2. 測定結果を `benchmarks/results/2026-07-26/sq9-v620-viability/` 配下に、    リポジトリの既存 evidence 慣行に従って保存すること（生データ + サマリ）。 3. `journal/2026/07/26/` に house style    （`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録すること。 4. 結論が `SQ9_0` に不利な場合は、`docs/plans/sq9-format-design-input-v0.1.md` に    実測に基づく追記を行うこと（設計文書自体の書き換えではなく、実測結果セクションの追加）。  推測で埋めないこと。実際に測定した数値だけを結論の根拠にし、 測定できなかった項目は「未測定」と明記すること。  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、`ullm-openai.service` のライフサイクルとユニット内容 - R9700 (gfx1201) 上での測定実行 - SQ8_0/AQ4_0 の campaign・authorization・candidate・release、`/opt/ullm` 配下（読み取りのみ可） - 既存 release ディレクトリ（`uLLM-*-release-*`）と既存ビルドツリー（`uLLM-*-build-*`）の内容 - 既存の `SQ8_0` / `AQ4_0` の本番実装コード（ベンチ用に読むのは自由、変更は禁止）  ## 並行実行に関する注意（重要）  他の codex セッションが**同じ worktree で同時に別タスクを実行している**。以下を厳守すること。  - `git add` は自分が作成/変更したファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら、数秒待って再試行する。lock ファイルを削除してはならない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。 - 自分の担当範囲外のファイルが作業ツリーに現れても、触らない・コミットに含めない。 - HEAD が自分の作業中に進んでいても正常である。rebase や force は行わない。  ## 報告  完了後に、(1) 測定に使ったデバイスの `gcnArchName` と PCI bus id、(2) M=1 での `SQ9_0` 対 `SQ8_0` の 実測比較（時間・達成帯域・効率）、(3) `SQ8_0` の dequant が ALU 律速かどうかの直接的証拠、 (4) 帯域律速から演算律速へ移る M の境界、(5) 採算条件 +7.29% を満たすかの判定、 (6) コミットハッシュ、を簡潔に報告すること。
 696093 homelab1 codex           codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼G: CDNA3 移植で「CK の gfx942 XDL インスタンス再利用」が成立するかの評価  ## 運用前提（全タスク共通）  - このマシン `homelab1-WRX80-Creator` は外部にサービス提供しておらず、本プロジェクトの開発専用である。   sudo（パスワード `Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）、GPU使用、   サービスの停止/起動は事前承認済みで、都度の確認は不要。 - ただし **final activation は人間の明示承認が必要**。本タスクでは一切行わない。 - 命名規約: `SQ8` はカテゴリ、`SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - CDNA3 実機は本マシンに無い。オフラインコンパイルと静的解析のみ可能。  ## 背景  直前の Phase 0 調査（コミット `794fd245`、`docs/plans/sq8-cdna3-port-plan-v0.1.md`）で次が判明した。  - 最適化された独立 `SQ8_0` projection は手書き WMMA ではなく、   `runtime/src/sq8_ck_gfx1201.hip.cpp` の Composable Kernel (CK)   `ck::tensor_operation::device::DeviceGemmMultipleD_ABScale` +   `DeviceGemmXdlUniversal` インスタンスであり、gfx1201 に固定されている。 - gfx942 の MFMA FP8 命令（`v_mfma_f32_16x16x32_fp8_fp8` / `32x32x16`）は実在を ISA で確認済み。 - ただし gfx942 MFMA は **FNUZ** 入力、canonical `SQ8_0` は **OCP E4M3FN** raw payload + `[128,128]` scale であり、   raw payload の直接投入は不可。`0x80 → 0x00` 正規化と scale 補正（片側 ×2、両側変換なら積 ×4）を伴う   派生 FNUZ prepack が必要と判定されている。  現行 plan は性能本命として「**native CDNA3 MFMA body を新規設計する**」（tile/fragment layout も自前で設計） を置いている。しかし次の観察がある。  **`Xdl` は XDLOPs、すなわち MFMA、すなわち CDNA の行列コアを指す名称である。** `DeviceGemmXdlUniversal` は CK の CDNA 系 GEMM ファミリであり、CDNA3 こそが本来のホームグラウンドである。 実際 ROCm 7.2.1 の CK ヘッダは gfx942/gfx90a を参照している （`/opt/rocm/include/ck/ck.hpp`、`/opt/rocm/include/ck/tensor_operation/gpu/grid/gridwise_gemm_xdlops_v3r3.hpp` 等）。  したがって「手書き MFMA body を新規設計する」より、 **同じ CK device op を gfx942 向けにインスタンス化し直す**方が、はるかに短い経路になる可能性がある。  ## 評価してほしい問い  1. **CK の `DeviceGemmMultipleD_ABScale` は gfx942 でインスタンス化できるか。**    現行 gfx1201 実装が使っている device op・データ型・tile 契約（`1,128,128`）・    ABScale のスケール適用形と**同等の構成**が、ROCm 7.2.1 の CK で gfx942 向けに存在するかを、    実際に CK のヘッダとインスタンス定義を読んで確認すること。存在しないなら、    最も近い gfx942 対応 device op は何で、契約がどうずれるかを具体的に示すこと。 2. **オフライン実証。** 隔離した作業ディレクトリで、`--offload-arch=gfx942` 向けに    その CK 構成を実際にコンパイルしてみること。成功したら生成 ISA に    `v_mfma` 系が出ているかを逆アセンブルで確認し、VGPR/SGPR/LDS/spill/occupancy を記録すること。    失敗したらエラーの正確な原因（型・アーキ制約・インスタンス欠如・feature 要求）を特定すること。    **本番ビルドツリーと既存 release を汚さないこと。** 3. **`ck::f8_t` の意味論。** CK の `f8_t` が gfx942 と gfx12 でそれぞれ    FNUZ / OCP のどちらに解決されるかを、ヘッダを読んで確定すること。    これが OCP→FNUZ prepack の必要性と、必要なら適用箇所（重み側・活性側・スケール側）を決める。 4. **判定と比較。** 次の3案を、実装量・リスク・性能見込み・実機依存度で比較すること。    - A: 手書き native MFMA body の新規設計（現行 plan の本命）    - **A′: CK の gfx942 XDL インスタンス再利用**（本タスクで評価する新案）    - B: dequant-to-FP16/BF16 の control 経路（正当性の対照）    いずれも OCP→FNUZ prepack の課題は共通で残ることを踏まえて評価すること。  ## 成果物  `docs/plans/sq8-cdna3-port-plan-v0.1.md` に**追記セクション**を加えること （既存記述の書き換えではなく、評価結果の追加）。A′ が成立するなら plan の Decision Tree と Phase 構成にどう影響するかも明記すること。成立しないなら、その根拠を具体的に残すこと。  あわせて `journal/2026/07/26/` に house style （`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で実測結果を記録すること。  推測で埋めないこと。実際に CK ヘッダを読み、コンパイルを試し、ISA を確認した事実に基づき、 確認できなかった点は「未確認」と明記すること。**A′ を成立させることが目的ではない。** 成立しないならそれを明確に報告すること。  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、`ullm-openai.service` のライフサイクルとユニット内容 - SQ8_0/AQ4_0 の campaign・authorization・candidate・release、`/opt/ullm` 配下（読み取りのみ可） - 既存 release ディレクトリ（`uLLM-*-release-*`）と既存ビルドツリー（`uLLM-*-build-*`）の内容 - 本番 RDNA4 経路の既存シンボルの外部 ABI/dispatch、`runtime/src/sq8_ck_gfx1201.hip.cpp` 本体 - R9700 (gfx1201) 上での測定実行、`llama-qwen35-udq4.service`（inactive/disabled 維持）  ## 並行実行に関する注意（重要）  他の codex セッションが**同じ worktree で同時に別タスクを実行している**。以下を厳守すること。  - `git add` は自分が作成/変更したファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら、数秒待って再試行する。lock ファイルを削除してはならない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。 - 自分の担当範囲外のファイルが作業ツリーに現れても、触らない・コミットに含めない。 - HEAD が自分の作業中に進んでいても正常である。rebase や force は行わない。  ## 報告  完了後に、(1) CK gfx942 インスタンスの有無と契約の一致度、(2) オフラインコンパイルの結果と ISA に `v_mfma` が出たか、(3) `ck::f8_t` のアーキ別解決、(4) A / A′ / B の比較判定、 (5) コミットハッシュ、を簡潔に報告すること。
 722702 homelab1 bash            /bin/bash -c result_dir="$PWD/benchmarks/results/2026-07-26/sq9-v620-viability" mkdir -p "$result_dir/raw" "$result_dir/build" "$result_dir/telemetry" amd-smi metric -u -p -c -t -g 0000:03:00.0 --json > "$result_dir/telemetry/preflight-amd-smi.json" amd-smi static -v -b -C MEM -g 0000:03:00.0 --json > "$result_dir/telemetry/device-static-amd-smi.json" {   systemctl is-active ullm-openai.service   systemctl is-enabled ullm-openai.service   systemctl is-active llama-qwen35-udq4.service   systemctl is-enabled llama-qwen35-udq4.service } > "$result_dir/telemetry/preflight-services.txt" ps -eo pid,user,comm,args | rg -i 'ullm-aq4-worker|llama-qwen35-udq4|ullm-openai' > "$result_dir/telemetry/preflight-relevant-processes.txt" || true "$result_dir/build/bench-sq9-v620-viability-hip" --pci-bus-id 0000:03:00.0 --suite smoke --warmups 2 --trials 3 --launches-per-trial 2 > "$result_dir/raw/smoke.jsonl" 2> "$result_dir/raw/smoke.stderr"
 722711 homelab1 rg              rg -i ullm-aq4-worker|llama-qwen35-udq4|ullm-openai
