2695971 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'cat > /tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad/codex-BJ-recheck-candidates.md <<'"'"'PROMPT_EOF'"'"' # 依頼BJ: 過去に却下された最適化候補を、新しい昇格方針で測り直す  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  **2026-07-26 に昇格方針が変更された。** `docs/plans/lightweight-promotion-policy-v0.1.md`（コミット `ebf9a545`）を**必ず先に読むこと**。  - 人間の承認は不要。 - 判定は **実際の推論による出力文章の品質が大きく崩れないこと**。 - **top-1 一致率や logits の厳密一致を合否閾値にしないこと。** - 昇格は `tools/promote-served-model.py` / `tools/rollback-promoted-served-model.py`   （コミット `d33b6772` 他）を使う。候補固有の apparatus を作らない。  **旧方針の副作用が重要である。** 旧運用は「不合格の候補は速度を測らない」だったため、 **却下された候補が速かったのかどうかすら記録されていない**ものがある。 新方針では測ってよい。**これが本タスクの狙いである。**  さらに、依頼BE が split-KV の数値性質を最小構成で切り分けた結果、 1036/128 の 9 tile で最大差は **`1.08033e-7`**（F32 で約 1 ULP）にすぎず、 初期化・tail/empty tile・ページ境界・merge scale 漏れは**すべて否定**された。 つまり **これまでの不合格の多くは、候補が壊れていたのではなく基準側の問題だった可能性が高い。**  ## 対象（本タスクの担当範囲）  **attention 系の候補は 依頼BH が担当している。本タスクでは触らないこと。** 本タスクは **射影 / フォーマット系**を担当する。  1. **手書き WMMA 射影**（`runtime/src/sq8_handwritten_gfx1201.hip.cpp`、    `ullm_sq8_handwritten_gfx1201_m1_wmma_kernel`）    - コンポーネント検証 4/4 通過、full-model で step 1 から不一致。    - 局在: layer 3、5,120 要素中 **2 要素**、最大絶対誤差 **`6.1035156e-5`**、      K128 block 1 の 8 番目の K16 サブブロック。    - **速度は未測定である。** 新方針では測ってよい。    - 射影は decode カーネル時間の **40.10%** を占める（CK GEMM 3 種の合計）ので、      ここが速くなる価値は大きい。 2. **`SQ8_1` W8A8**（int8 + per-K=32 FP16 スケール、8.5 bpp）    - 旧判定: top-1 98.727% < 99%、logits 最大絶対 7.889 > 1.0 で不合格。    - **注意: logits 最大絶対 7.889 は大きい。** 1 ULP の話ではない。      **これは本当に品質が崩れている可能性がある。**軽量判定で実際の生成文を見て判断すること。      方針が緩んだことを、壊れたものを通す口実にしないこと。 3. 他に却下済みの射影/フォーマット系候補があれば洗い出すこと。    `benchmarks/results/` と `docs/plans/`、`journal/` を調べること。    BD の報告によれば**登録候補が 5 件あり、4 件が静的 validator を通過している**。    その実体を確認すること。  ## 手順  各候補について:  1. **まず速度を測ること。** 速くないなら、数値がどうであれ昇格する意味がない。    そこで打ち切ってよい（「速くなかった」と記録すること）。 2. 速いなら、**軽量判定を実行すること**（固定プロンプト集で実際に生成し、証跡を保存）。 3. **現行本番の出力と並べて保存すること。** 4. 基準を満たすなら**昇格まで実施すること。** 満たさないなら、    **生成文のどこがどう壊れていたか**を具体的に記録すること    （「top-1 が閾値未満」ではなく、実際の出力の何が問題か）。 5. **full-model の decode tok/s で効果を示すこと。** カーネル単体の倍率で判断しないこと。    有効な既存ベースラインは **15.294955751 tok/s**（65.381033 ms/token）、    比較対象 llama.cpp は **30.4680750229 tok/s**。  ## 計測の作法（重要）  - **依頼BH が R9700 で排他計測窓を使っている。** 計測前に必ず確認すること:   ```   pgrep -af '"'"'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model|ullm-aq4-worker'"'"'   systemctl is-active ullm-openai.service   ```   **本番 worker が動いている状態で GPU タイミングを取らないこと。**   依頼BE の full-model 計測は `ullm-openai.service` の復帰と競合して汚染され、無効化された。 - `ullm-openai.service` の停止が必要なら stop → isolated → restore。**終了後は必ず復旧すること。**   `StartLimitBurst=3` / `StartLimitIntervalSec=900`。**窓の回数を最小化すること。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - 温度・クロック・throttle を記録し、クールダウンで開始温度を揃えること。 - **BH と昇格が競合しうる。** 昇格する前に現在の active manifest を確認し、   自分が起点だと思っていたものと違っていたら、**上書きせずに報告すること。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` / `SQ8_1` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却）。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - CPU 64 コアの F32 参照コーパスは**停止済み**。CPU に余裕がある。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 他セッションとの競合  **依頼BF**（ローダー config 駆動化）、**依頼BG**（worker EOF 調査）、 **依頼BH**（decode attention 再設計）、**依頼BI**（MoE ランタイム）が並行して動いている。  - `crates/ullm-engine/src/qwen3_loader.rs` およびローダー周辺 → BF - `runtime/src/kernels/` の attention 系 → BH - MoE 関連の新規ファイル → BI - **これらを触らないこと。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`（昇格ツール経由でのみ変更すること） - systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `tools/promote-served-model.py`、`tools/rollback-promoted-served-model.py`   （**使ってよいが変更しないこと**）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - 候補ごとの**速度測定結果**（これまで測られていなかったもの） - 軽量判定の生成文証跡（現行本番との並置） - 昇格した場合はその記録、しなかった場合は**具体的に何が壊れていたか** - 結果を `benchmarks/results/2026-07-26/candidate-recheck-lightweight/` に保存 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **手書き WMMA 射影は速かったのか**（これまで未測定だったもの）、 (2) `SQ8_1` W8A8 の生成文は実際に壊れているか（logits 最大絶対 7.889 の実害）、 (3) 洗い出した他の候補とその結果、 (4) **昇格したものがあるか**、あるなら full-model decode tok/s の変化、 (5) 昇格しなかったものについて、生成文の具体的な破綻内容、 (6) 計測窓の回数とサービス復旧の記録、現在の active manifest SHA-256、 (7) コミットハッシュ。 PROMPT_EOF S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad && codex exec -m gpt-5.6-terra -c model_reasoning_effort="max" -c service_tier="priority" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-BJ-recheck-candidates.md)" < /dev/null 2>&1 | tail -30' && pwd -P >| /tmp/claude-a020-cwd
2695974 codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼BJ: 過去に却下された最適化候補を、新しい昇格方針で測り直す  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  **2026-07-26 に昇格方針が変更された。** `docs/plans/lightweight-promotion-policy-v0.1.md`（コミット `ebf9a545`）を**必ず先に読むこと**。  - 人間の承認は不要。 - 判定は **実際の推論による出力文章の品質が大きく崩れないこと**。 - **top-1 一致率や logits の厳密一致を合否閾値にしないこと。** - 昇格は `tools/promote-served-model.py` / `tools/rollback-promoted-served-model.py`   （コミット `d33b6772` 他）を使う。候補固有の apparatus を作らない。  **旧方針の副作用が重要である。** 旧運用は「不合格の候補は速度を測らない」だったため、 **却下された候補が速かったのかどうかすら記録されていない**ものがある。 新方針では測ってよい。**これが本タスクの狙いである。**  さらに、依頼BE が split-KV の数値性質を最小構成で切り分けた結果、 1036/128 の 9 tile で最大差は **`1.08033e-7`**（F32 で約 1 ULP）にすぎず、 初期化・tail/empty tile・ページ境界・merge scale 漏れは**すべて否定**された。 つまり **これまでの不合格の多くは、候補が壊れていたのではなく基準側の問題だった可能性が高い。**  ## 対象（本タスクの担当範囲）  **attention 系の候補は 依頼BH が担当している。本タスクでは触らないこと。** 本タスクは **射影 / フォーマット系**を担当する。  1. **手書き WMMA 射影**（`runtime/src/sq8_handwritten_gfx1201.hip.cpp`、    `ullm_sq8_handwritten_gfx1201_m1_wmma_kernel`）    - コンポーネント検証 4/4 通過、full-model で step 1 から不一致。    - 局在: layer 3、5,120 要素中 **2 要素**、最大絶対誤差 **`6.1035156e-5`**、      K128 block 1 の 8 番目の K16 サブブロック。    - **速度は未測定である。** 新方針では測ってよい。    - 射影は decode カーネル時間の **40.10%** を占める（CK GEMM 3 種の合計）ので、      ここが速くなる価値は大きい。 2. **`SQ8_1` W8A8**（int8 + per-K=32 FP16 スケール、8.5 bpp）    - 旧判定: top-1 98.727% < 99%、logits 最大絶対 7.889 > 1.0 で不合格。    - **注意: logits 最大絶対 7.889 は大きい。** 1 ULP の話ではない。      **これは本当に品質が崩れている可能性がある。**軽量判定で実際の生成文を見て判断すること。      方針が緩んだことを、壊れたものを通す口実にしないこと。 3. 他に却下済みの射影/フォーマット系候補があれば洗い出すこと。    `benchmarks/results/` と `docs/plans/`、`journal/` を調べること。    BD の報告によれば**登録候補が 5 件あり、4 件が静的 validator を通過している**。    その実体を確認すること。  ## 手順  各候補について:  1. **まず速度を測ること。** 速くないなら、数値がどうであれ昇格する意味がない。    そこで打ち切ってよい（「速くなかった」と記録すること）。 2. 速いなら、**軽量判定を実行すること**（固定プロンプト集で実際に生成し、証跡を保存）。 3. **現行本番の出力と並べて保存すること。** 4. 基準を満たすなら**昇格まで実施すること。** 満たさないなら、    **生成文のどこがどう壊れていたか**を具体的に記録すること    （「top-1 が閾値未満」ではなく、実際の出力の何が問題か）。 5. **full-model の decode tok/s で効果を示すこと。** カーネル単体の倍率で判断しないこと。    有効な既存ベースラインは **15.294955751 tok/s**（65.381033 ms/token）、    比較対象 llama.cpp は **30.4680750229 tok/s**。  ## 計測の作法（重要）  - **依頼BH が R9700 で排他計測窓を使っている。** 計測前に必ず確認すること:   ```   pgrep -af 'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model|ullm-aq4-worker'   systemctl is-active ullm-openai.service   ```   **本番 worker が動いている状態で GPU タイミングを取らないこと。**   依頼BE の full-model 計測は `ullm-openai.service` の復帰と競合して汚染され、無効化された。 - `ullm-openai.service` の停止が必要なら stop → isolated → restore。**終了後は必ず復旧すること。**   `StartLimitBurst=3` / `StartLimitIntervalSec=900`。**窓の回数を最小化すること。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - 温度・クロック・throttle を記録し、クールダウンで開始温度を揃えること。 - **BH と昇格が競合しうる。** 昇格する前に現在の active manifest を確認し、   自分が起点だと思っていたものと違っていたら、**上書きせずに報告すること。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` / `SQ8_1` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却）。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - CPU 64 コアの F32 参照コーパスは**停止済み**。CPU に余裕がある。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 他セッションとの競合  **依頼BF**（ローダー config 駆動化）、**依頼BG**（worker EOF 調査）、 **依頼BH**（decode attention 再設計）、**依頼BI**（MoE ランタイム）が並行して動いている。  - `crates/ullm-engine/src/qwen3_loader.rs` およびローダー周辺 → BF - `runtime/src/kernels/` の attention 系 → BH - MoE 関連の新規ファイル → BI - **これらを触らないこと。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`（昇格ツール経由でのみ変更すること） - systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `tools/promote-served-model.py`、`tools/rollback-promoted-served-model.py`   （**使ってよいが変更しないこと**）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - 候補ごとの**速度測定結果**（これまで測られていなかったもの） - 軽量判定の生成文証跡（現行本番との並置） - 昇格した場合はその記録、しなかった場合は**具体的に何が壊れていたか** - 結果を `benchmarks/results/2026-07-26/candidate-recheck-lightweight/` に保存 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **手書き WMMA 射影は速かったのか**（これまで未測定だったもの）、 (2) `SQ8_1` W8A8 の生成文は実際に壊れているか（logits 最大絶対 7.889 の実害）、 (3) 洗い出した他の候補とその結果、 (4) **昇格したものがあるか**、あるなら full-model decode tok/s の変化、 (5) 昇格しなかったものについて、生成文の具体的な破綻内容、 (6) 計測窓の回数とサービス復旧の記録、現在の active manifest SHA-256、 (7) コミットハッシュ。
3221294 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'cat > /tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad/codex-BP-sq8-generalize.md <<'"'"'PROMPT_EOF'"'"' # 依頼BP: `SQ8_0` resident ランタイムの Qwen3-14B 決め打ち解消  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  本日、アーキテクチャ側で 2 つが「正しく動く」状態に到達した。  - **Gemma4 E2B**（依頼BL `b4e02f92`/`c0e5e428`、依頼BO `0c6ae998`/`44d9ab1c`）:   HF と greedy トークン列が完全一致。resident BF16 で KV キャッシュ・sliding window・   layer 15 以降の KV 共有まで検証済み（陰性対照つき）。   ただし速度は llama.cpp に **prefill 12.0 倍 / decode 4.5 倍**負けている   （uLLM prefill 18.296 tok/s、decode 15.613 tok/s 対 llama.cpp 218.956 / 69.960）。 - **Qwen3.5-35B-A3B MoE**（依頼BI `b1801dc6` ほか）: routing / gather-scatter /   grouped GEMM が HF 照合済み。ただし BF16 では VRAM に載らず、量子化が必須。  **両者に共通する壁が同じである。** 依頼BL の記録:  > 既存 `SQ8_0` runtime は Qwen3-14B の fixed width/head/intermediate/norm/RoPE/layer array を > 前提にしており、Gemma4 の mixed-width local/full attention、PLE、shared K/V、tied head、 > soft-cap を表せない。  **本タスクは、この決め打ちを解消して、量子化 resident 経路を複数アーキテクチャで使えるようにすることである。**  ## 前提として読むこと  - 依頼BF の config 駆動化（`crates/ullm-engine/src/model_config.rs`、`b21b2723`/`b3d78b42`）。   **既に config から descriptor を組み立てる仕組みがある。** それを土台にすること。 - `docs/plans/multi-architecture-support-plan-v0.1.md`（Qwen3 固有の固定契約 15 箇所の棚卸しあり） - `docs/plans/qwen35-moe-runtime-foundation-v0.1.md`（依頼BI の MoE 設計） - `journal/2026/07/26/gemma4-e2b-text-executor.md` と   `journal/2026/07/26/gemma4-e2b-resident-bf16.md`（Gemma4 の要求事項）  ## Phase 1: 決め打ちの棚卸し  **`SQ8_0` の resident 実行経路のどこが Qwen3-14B 固定かを、ファイル・関数レベルで列挙すること。**  対象と見られるもの（**自分で確認すること。これで網羅とは限らない**）:  - `crates/ullm-engine/src/sq8_layer_runtime.rs` - `crates/ullm-engine/src/sq8_stack_runtime.rs` - `crates/ullm-engine/src/sq8_model_head_runtime.rs` - 関連する package / artifact のスキーマ  各項目について、**何が固定されていて、一般化すると何が必要になるか**を書くこと。 特に:  - hidden size / head 数 / KV head 数 / head_dim / intermediate size - 層数と、層ごとに構成が変わる可能性（Gemma4 は local/full 交互、MoE は dense/MoE 混在） - 正規化の数と位置（Gemma4 は **4 つの residual norm**） - RoPE の種類（Gemma4 は local/full で別、proportional RoPE） - **KV 共有**（Gemma4 は layer 15 以降が layer 13/14 を共有） - **sliding window**（local 層は窓外を落とす） - tied embedding、embedding scale、logit soft-cap - **PLE**（per-layer embeddings）  ## Phase 2: 設計  1. **descriptor 駆動にすること。** 依頼BF が作った config → descriptor の流れに乗せ、    resident ランタイムが descriptor を見て構成を決めるようにすること。 2. **過度に汎用化しないこと。** 読めなくなるくらいなら、    アーキテクチャごとの分岐を明示的に持つ方がよい。判断を記録すること。 3. **既存 Qwen3-14B `SQ8_0` の挙動を変えないことが最優先。** これは本番相当の経路である。 4. **Gemma4 と MoE のどちらを先に通すかを決めること。**    - Gemma4 E2B は BF16 で 9.54 GiB、**量子化しなくても R9700 に載る**。      つまり Gemma4 に必要なのは「量子化」ではなく「resident 経路の一般化と速度」である。    - MoE は 63.613 GiB で**量子化が必須**（依頼BN が `AQ4_0` 量子化を進行中）。    - **この違いを踏まえて優先順位を決め、理由を書くこと。**  ## Phase 3: 実装と検証  1. **既存 Qwen3-14B `SQ8_0` の回帰確認を最初に行うこと。**    一般化の前後で、同じ入力から同じ出力が出ること。**ここが壊れたら本タスクは失敗である。** 2. 一般化した経路で、**少なくとも 1 つの新アーキテクチャを通すこと。** 3. 検証は `tools/architecture_hf_trace.py` と、    BL/BO が残した greedy トークン列の証跡    （`benchmarks/results/2026-07-26/gemma4-e2b-nonquantized-v0.1/`、    `benchmarks/results/2026-07-26/gemma4-e2b-resident-v0.1/`）を使うこと。    **これらは読み取りのみ。** 4. **厳密な数値閾値を合否に使わないこと。**    本プロジェクトでは、閾値を誤った場面に当てて良い成果を落とす失敗が繰り返されている    （v0.2 ゲートで 4 候補、量子化モデルを BF16 参照と比較して 42/43 テンソル fail など）。    判定は `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従い、    **greedy トークン列と実際の生成文**で見ること。    **FP32 参照コーパス機構・ビット一致ゲート・campaign 機構は使わないこと。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` / `SQ8_1` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。   `HIP_VISIBLE_DEVICES` で R9700 に固定すること。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - CPU は空いている（F32 参照コーパスは停止済み）。ビルドの並列度は上げてよい。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 3 本が並行して動いており、GPU と本番サービスが逼迫している。**  - **依頼BH**（decode attention 再設計）が **R9700 の排他ロック `/run/ullm/r9700.lock` を   flock で保持し、`ullm-openai.service` を停止して full-model 計測中**である。 - **`ullm-openai.service` は現在 `failed` / start-limit 状態**である   （`NRestarts=3`、`StartLimitBurst=3` / `StartLimitIntervalSec=900`）。   別セッションが BH のロック保持中に起動を試みて `WorkerBusy` で失敗した結果である。 - **依頼BJ**（却下候補の再評価）と **依頼BM**（`AQ4_0` P3 デプロイ）も GPU と昇格を待っている。  したがって:  - **GPU を使う前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af '"'"'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'"'"'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **ロックが保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` を起動しようとしないこと。** 復旧は別途行われる。   再起動予算を消費しないこと。 - **Phase 1 と Phase 2 は GPU 不要である。先にそちらを全部進めること。**  ### 他セッションが編集中のファイル（触らないこと）  - **BH**: `runtime/src/ullm_runtime_parts/part_01.inc`、`runtime/src/ullm_runtime_hiprtc_sources.inc` - **BJ**: `runtime/src/sq8_handwritten_gfx1201.hip.cpp`、`SQ8_1` 関連 - **BN**: MoE の量子化パイプライン、`docs/plans/qwen35-moe-runtime-foundation-v0.1.md` - **BM**: `AQ4_0` の release / 昇格まわり  **新しいカーネルが必要になった場合、`part_01.inc` に足さないこと**（BH と衝突する）。 新規ファイルにするか、BH の完了を待つこと。**判断を記録すること。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `benchmarks/results/2026-07-26/gemma4-e2b-*`（BL/BO の証跡。**読み取りのみ**）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - **Qwen3-14B 決め打ち箇所の棚卸し**（ファイル・関数レベル、一般化に必要なものつき） - descriptor 駆動の設計と、汎用化の境界をどこに引いたかの説明 - **既存 Qwen3-14B `SQ8_0` の回帰確認証跡** - 一般化した経路で新アーキテクチャを通した証跡（greedy トークン列で） - `docs/plans/multi-architecture-support-plan-v0.1.md` に進捗を追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) Qwen3-14B 決め打ちは何箇所あり、何が問題だったか、 (2) 汎用化の境界をどこに引いたか（と、その理由）、 (3) **既存 Qwen3-14B `SQ8_0` は壊れていないか**、 (4) Gemma4 と MoE のどちらを先にしたか、その理由、 (5) 新アーキテクチャが一般化経路を通ったか（greedy トークン列は一致したか）、 (6) GPU ロックと本番サービスに触れずに済んだか、 (7) コミットハッシュ。 PROMPT_EOF S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad && codex exec -m gpt-5.6-terra -c model_reasoning_effort="max" -c service_tier="priority" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-BP-sq8-generalize.md)" < /dev/null 2>&1 | tail -30' && pwd -P >| /tmp/claude-56b1-cwd
3221297 codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼BP: `SQ8_0` resident ランタイムの Qwen3-14B 決め打ち解消  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  本日、アーキテクチャ側で 2 つが「正しく動く」状態に到達した。  - **Gemma4 E2B**（依頼BL `b4e02f92`/`c0e5e428`、依頼BO `0c6ae998`/`44d9ab1c`）:   HF と greedy トークン列が完全一致。resident BF16 で KV キャッシュ・sliding window・   layer 15 以降の KV 共有まで検証済み（陰性対照つき）。   ただし速度は llama.cpp に **prefill 12.0 倍 / decode 4.5 倍**負けている   （uLLM prefill 18.296 tok/s、decode 15.613 tok/s 対 llama.cpp 218.956 / 69.960）。 - **Qwen3.5-35B-A3B MoE**（依頼BI `b1801dc6` ほか）: routing / gather-scatter /   grouped GEMM が HF 照合済み。ただし BF16 では VRAM に載らず、量子化が必須。  **両者に共通する壁が同じである。** 依頼BL の記録:  > 既存 `SQ8_0` runtime は Qwen3-14B の fixed width/head/intermediate/norm/RoPE/layer array を > 前提にしており、Gemma4 の mixed-width local/full attention、PLE、shared K/V、tied head、 > soft-cap を表せない。  **本タスクは、この決め打ちを解消して、量子化 resident 経路を複数アーキテクチャで使えるようにすることである。**  ## 前提として読むこと  - 依頼BF の config 駆動化（`crates/ullm-engine/src/model_config.rs`、`b21b2723`/`b3d78b42`）。   **既に config から descriptor を組み立てる仕組みがある。** それを土台にすること。 - `docs/plans/multi-architecture-support-plan-v0.1.md`（Qwen3 固有の固定契約 15 箇所の棚卸しあり） - `docs/plans/qwen35-moe-runtime-foundation-v0.1.md`（依頼BI の MoE 設計） - `journal/2026/07/26/gemma4-e2b-text-executor.md` と   `journal/2026/07/26/gemma4-e2b-resident-bf16.md`（Gemma4 の要求事項）  ## Phase 1: 決め打ちの棚卸し  **`SQ8_0` の resident 実行経路のどこが Qwen3-14B 固定かを、ファイル・関数レベルで列挙すること。**  対象と見られるもの（**自分で確認すること。これで網羅とは限らない**）:  - `crates/ullm-engine/src/sq8_layer_runtime.rs` - `crates/ullm-engine/src/sq8_stack_runtime.rs` - `crates/ullm-engine/src/sq8_model_head_runtime.rs` - 関連する package / artifact のスキーマ  各項目について、**何が固定されていて、一般化すると何が必要になるか**を書くこと。 特に:  - hidden size / head 数 / KV head 数 / head_dim / intermediate size - 層数と、層ごとに構成が変わる可能性（Gemma4 は local/full 交互、MoE は dense/MoE 混在） - 正規化の数と位置（Gemma4 は **4 つの residual norm**） - RoPE の種類（Gemma4 は local/full で別、proportional RoPE） - **KV 共有**（Gemma4 は layer 15 以降が layer 13/14 を共有） - **sliding window**（local 層は窓外を落とす） - tied embedding、embedding scale、logit soft-cap - **PLE**（per-layer embeddings）  ## Phase 2: 設計  1. **descriptor 駆動にすること。** 依頼BF が作った config → descriptor の流れに乗せ、    resident ランタイムが descriptor を見て構成を決めるようにすること。 2. **過度に汎用化しないこと。** 読めなくなるくらいなら、    アーキテクチャごとの分岐を明示的に持つ方がよい。判断を記録すること。 3. **既存 Qwen3-14B `SQ8_0` の挙動を変えないことが最優先。** これは本番相当の経路である。 4. **Gemma4 と MoE のどちらを先に通すかを決めること。**    - Gemma4 E2B は BF16 で 9.54 GiB、**量子化しなくても R9700 に載る**。      つまり Gemma4 に必要なのは「量子化」ではなく「resident 経路の一般化と速度」である。    - MoE は 63.613 GiB で**量子化が必須**（依頼BN が `AQ4_0` 量子化を進行中）。    - **この違いを踏まえて優先順位を決め、理由を書くこと。**  ## Phase 3: 実装と検証  1. **既存 Qwen3-14B `SQ8_0` の回帰確認を最初に行うこと。**    一般化の前後で、同じ入力から同じ出力が出ること。**ここが壊れたら本タスクは失敗である。** 2. 一般化した経路で、**少なくとも 1 つの新アーキテクチャを通すこと。** 3. 検証は `tools/architecture_hf_trace.py` と、    BL/BO が残した greedy トークン列の証跡    （`benchmarks/results/2026-07-26/gemma4-e2b-nonquantized-v0.1/`、    `benchmarks/results/2026-07-26/gemma4-e2b-resident-v0.1/`）を使うこと。    **これらは読み取りのみ。** 4. **厳密な数値閾値を合否に使わないこと。**    本プロジェクトでは、閾値を誤った場面に当てて良い成果を落とす失敗が繰り返されている    （v0.2 ゲートで 4 候補、量子化モデルを BF16 参照と比較して 42/43 テンソル fail など）。    判定は `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従い、    **greedy トークン列と実際の生成文**で見ること。    **FP32 参照コーパス機構・ビット一致ゲート・campaign 機構は使わないこと。**  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` / `SQ8_1` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。   `HIP_VISIBLE_DEVICES` で R9700 に固定すること。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - CPU は空いている（F32 参照コーパスは停止済み）。ビルドの並列度は上げてよい。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 3 本が並行して動いており、GPU と本番サービスが逼迫している。**  - **依頼BH**（decode attention 再設計）が **R9700 の排他ロック `/run/ullm/r9700.lock` を   flock で保持し、`ullm-openai.service` を停止して full-model 計測中**である。 - **`ullm-openai.service` は現在 `failed` / start-limit 状態**である   （`NRestarts=3`、`StartLimitBurst=3` / `StartLimitIntervalSec=900`）。   別セッションが BH のロック保持中に起動を試みて `WorkerBusy` で失敗した結果である。 - **依頼BJ**（却下候補の再評価）と **依頼BM**（`AQ4_0` P3 デプロイ）も GPU と昇格を待っている。  したがって:  - **GPU を使う前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af 'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **ロックが保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` を起動しようとしないこと。** 復旧は別途行われる。   再起動予算を消費しないこと。 - **Phase 1 と Phase 2 は GPU 不要である。先にそちらを全部進めること。**  ### 他セッションが編集中のファイル（触らないこと）  - **BH**: `runtime/src/ullm_runtime_parts/part_01.inc`、`runtime/src/ullm_runtime_hiprtc_sources.inc` - **BJ**: `runtime/src/sq8_handwritten_gfx1201.hip.cpp`、`SQ8_1` 関連 - **BN**: MoE の量子化パイプライン、`docs/plans/qwen35-moe-runtime-foundation-v0.1.md` - **BM**: `AQ4_0` の release / 昇格まわり  **新しいカーネルが必要になった場合、`part_01.inc` に足さないこと**（BH と衝突する）。 新規ファイルにするか、BH の完了を待つこと。**判断を記録すること。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `benchmarks/results/2026-07-26/gemma4-e2b-*`（BL/BO の証跡。**読み取りのみ**）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - **Qwen3-14B 決め打ち箇所の棚卸し**（ファイル・関数レベル、一般化に必要なものつき） - descriptor 駆動の設計と、汎用化の境界をどこに引いたかの説明 - **既存 Qwen3-14B `SQ8_0` の回帰確認証跡** - 一般化した経路で新アーキテクチャを通した証跡（greedy トークン列で） - `docs/plans/multi-architecture-support-plan-v0.1.md` に進捗を追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) Qwen3-14B 決め打ちは何箇所あり、何が問題だったか、 (2) 汎用化の境界をどこに引いたか（と、その理由）、 (3) **既存 Qwen3-14B `SQ8_0` は壊れていないか**、 (4) Gemma4 と MoE のどちらを先にしたか、その理由、 (5) 新アーキテクチャが一般化経路を通ったか（greedy トークン列は一致したか）、 (6) GPU ロックと本番サービスに触れずに済んだか、 (7) コミットハッシュ。
3251339 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'cat > /tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad/codex-BR-prefill-attention.md <<'"'"'PROMPT_EOF'"'"' # 依頼BR: prefill attention に GQA 協調 + KV 分割を適用する  ## 背景: decode で 1.790 倍が取れた  依頼BH が decode attention を再設計し、**full-model で実測 1.790050 倍**を達成した （コミット `15907d84` / `0455b119` / `b65e63c3` / `3d914439`、 `benchmarks/results/2026-07-26/sq8-decode-attention-redesign/`）。  | 段階 | full-model tok/s | 直前比 | |---|---:|---:| | direct control | 15.228021 | — | | generic tile 128 | 22.412990 | 1.471826x | | generic tile 20 | 23.872855 | 1.065135x | | **GQA grouped tile 20** | **27.378731** | **1.146856x** | | barrier pipeline 追加 | 27.253517 | 0.995427x（不採用） |  **llama.cpp 30.468075 tok/s の 89.86% まで到達した。**  効いた要素:  - **GQA 協調**: 1 つの KV head を共有する 5 つの Q head を同一 WG で処理し、   KV を 1 回だけ読む。semantic K+V load が **42,434,560 B → 8,486,912 B**（5 分の 1）。 - **KV 分割**: tile 20 で 52 分割。WG 供給が 15.625% から大幅に改善。 - 数値差は `split_vs_direct` 最大絶対 **`1.08033e-7`**（F32 で約 1 ULP）に留まる。  **注意すべき教訓**: barrier pipeline は attention-only probe では 1.080422 倍改善したのに **full-model では 0.995427 倍と悪化**した。**probe 単体の倍率で採否を決めないこと。**  ## 本タスク: 同じ技法を prefill に当てる  依頼BK が prefill の M=1 tail 欠陥を解消した （コミット `17a531a2` / `c97ac7fa` / `bcf948b7`、 `benchmarks/results/2026-07-26/prefill-tail-fix/`）。4095 で 71.576 → 99.532 tok/s（1.391 倍）。  **しかし BK は残差の所在を明確に特定している:**  > tail 解消後の主残差は**長い causal prefix を持つ cached-prefix attention**であり、 > BH が作業中の attention kernel source は変更していません。  現状の prefill 実測（依頼AY、`benchmarks/results/2026-07-26/r9700-prefill-comparison/`、 BK 修正後の値）:  | prompt | uLLM | llama.cpp Q8_0 (F32 KV) | 差 | |---:|---:|---:|---:| | 128 | 865.278 | 1,165.756 | 1.347 倍 | | 512 | 519.872 | 1,195.722 | 2.300 倍 | | 1024 | 337.896 | 1,145.351 | 3.390 倍 | | 2048 | 189.685 | 1,058.379 | 5.580 倍 | | 4095 | 99.532 | 1,008.683 | **10.134 倍** |  **llama.cpp はほぼ平坦、uLLM だけが長さとともに崩落する。** prefill attention は O(N²) なので、attention カーネルが非効率なら**まさにこの形になる。**  **BH のファイルは解放された。** 対象カーネルは `runtime/src/ullm_runtime_parts/part_01.inc` と `runtime/src/ullm_runtime_hiprtc_sources.inc` にある:  - `ullm_cached_prefix_attn_f32_kernel` / `_flash2_kernel` - `ullm_cached_prefix_attn_fp8_e4m3_kernel` / `_flash2_kernel` / `_rocwmma_kernel` - `ullm_causal_attn_f32_kernel` / `_flash2_kernel` - `ullm_causal_attn_batch_f32_kernel` / `_flash2_kernel`  ## Phase 0: どれが本当に効いているかを測る  **decode で「generic matvec は本番で 1 回も起動していなかった」という事例がある。 prefill でも、まずどのカーネルが実際に時間を使っているかを測ること。**  1. **prefill の rocprof カーネル内訳を取ること**（プロンプト長を振って: 512 / 2048 / 4095）。    どのカーネルが何 % を占めるか。**長さによって内訳がどう変わるか。** 2. **起動ジオメトリと WG 数、wave 供給率**を、BH が decode で作った表と同じ形で出すこと。 3. **llama.cpp の prefill attention と並置比較すること。** 4. **崩落が本当に attention によるものかを確認すること。**    違うなら、そう報告すること。**私の想定に合わせないこと。**  ## Phase 1: 適用  Phase 0 の結果に従って設計すること。**decode の設計をそのまま貼らないこと。** prefill は M=N で decode と要求が違う:  - decode は 1 クエリ対 C 個の KV。prefill は N クエリ対 N（または prefix+N）個の KV。 - prefill は既に**タイル化された flash attention 系のカーネルがある**（`_flash2_`）。   **既存が何をしているかを読んでから足すこと。** - **GQA 協調が prefill でも成立するか**を確認すること   （decode では KV を 5 回読み直していた。prefill でも同じ構造か）。 - **BK の tail 修正（cursor rewind 方式）と衝突しないこと。**   BK は `crates/ullm-engine/src/sq8_serving_runtime.rs` を変更済みである。  ## Phase 2: 検証と計測  1. **数値**: BH と同じく、`1e-7` 規模（1 ULP）に収まることを確認すること。    **厳密な数値閾値を合否に使わないこと**（本プロジェクトでは閾値の誤用で    良い候補を落とす失敗が繰り返されている）。判定は    `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従うこと。 2. **速度は full-model の prefill tok/s で示すこと。**    **カーネル単体の倍率で採否を決めないこと**（BH の barrier pipeline の教訓）。    AY / BK と同一条件・同一会計で測ること    （`benchmarks/results/2026-07-26/r9700-prefill-comparison/conditions.md` と    `accounting.md`）。**128 / 512 / 1024 / 2048 / 4095 を測ること。** 3. **decode を壊していないことを確認すること。**    同じカーネルファイルを触るので、BH の 27.378731 tok/s が維持されることを確認すること。 4. **長さごとの改善率を出すこと。** 崩落カーブが平坦化したかが本質である。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。   `HIP_VISIBLE_DEVICES` で固定すること。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - 温度・クロック・throttle を記録し、クールダウンで開始温度を揃えること。   R9700 は junction crit 110 ℃、負荷時 97 ℃ 程度で最上位 DPM を維持する。   `amd-smi` の `THROTTLE_STATUS: THROTTLED` は具体的 violation が全て N/A であり、   それ自体はクロック低下の証拠ではない。**実クロックを見ること。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 4 本が並行して動いており、GPU 排他ロックと本番サービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）である。**   **GPU で計測する前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af '"'"'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'"'"'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` は `StartLimitBurst=3` / `StartLimitIntervalSec=900`。   本日既に 2 回 start-limit に当たっている。**窓の回数を最小化すること。   停止したら必ず復旧すること。 - **依頼BM が `AQ4_0` P3 を本番に昇格させた直後である**   （新 manifest SHA-256 `a98910dc5bf59dc768e5bcd20bcf58968699540eb1b33df33066dcb6f274fe49`、   worker `/opt/ullm/aq4-p3-deployment-v0.1/releases/aq4-p3-c4c9a9b3/ullm-aq4-worker`）。   **`active.json` を変更しないこと。本タスクは昇格を行わない。** - 他セッション: **BJ**（`SQ8_0` 射影 / `SQ8_1` 候補再評価）、**BN**（MoE 量子化）、   **BP**（`SQ8_0` resident ランタイムの一般化。   `sq8_layer_runtime.rs` / `sq8_stack_runtime.rs` / `sq8_model_head_runtime.rs`）。   **これらのファイルには触らないこと。** - **`crates/ullm-engine/src/sq8_serving_runtime.rs` は BK が変更したばかりである。**   変更が必要なら、BK の tail 修正を壊さないことを確認し、理由を記録すること。  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `benchmarks/results/2026-07-26/` の既存ディレクトリ（**読み取りのみ**。自分の分は新規に作ること）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - **Phase 0 の内訳**（プロンプト長ごとに、どのカーネルが何 %、WG 数、wave 供給率、   llama.cpp との並置） - 実装と、**長さごとの full-model prefill tok/s**（128 / 512 / 1024 / 2048 / 4095） - **decode 27.378731 tok/s が維持されていることの確認** - 数値差の記録（1 ULP 規模に収まるか） - 結果を `benchmarks/results/2026-07-26/prefill-attention-redesign/` に保存 - `docs/plans/sq8-r9700-handwritten-kernel-optimization-plan-v0.1.md` に追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **prefill の時間内訳**（本当に attention が崩落の原因か。違うなら何か）、 (2) GQA 協調が prefill でも成立したか、 (3) **長さごとの prefill tok/s と、崩落カーブが平坦化したか**（llama.cpp との比較）、 (4) **decode 27.378731 tok/s は維持されたか**、 (5) 数値差は 1 ULP 規模に収まったか、 (6) GPU ロックとサービス窓の使用記録、`active.json` に触れていないこと、 (7) コミットハッシュ。 PROMPT_EOF S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad && codex exec -m gpt-5.6-terra -c model_reasoning_effort="max" -c service_tier="priority" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-BR-prefill-attention.md)" < /dev/null 2>&1 | tail -30' && pwd -P >| /tmp/claude-b543-cwd
3251342 codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼BR: prefill attention に GQA 協調 + KV 分割を適用する  ## 背景: decode で 1.790 倍が取れた  依頼BH が decode attention を再設計し、**full-model で実測 1.790050 倍**を達成した （コミット `15907d84` / `0455b119` / `b65e63c3` / `3d914439`、 `benchmarks/results/2026-07-26/sq8-decode-attention-redesign/`）。  | 段階 | full-model tok/s | 直前比 | |---|---:|---:| | direct control | 15.228021 | — | | generic tile 128 | 22.412990 | 1.471826x | | generic tile 20 | 23.872855 | 1.065135x | | **GQA grouped tile 20** | **27.378731** | **1.146856x** | | barrier pipeline 追加 | 27.253517 | 0.995427x（不採用） |  **llama.cpp 30.468075 tok/s の 89.86% まで到達した。**  効いた要素:  - **GQA 協調**: 1 つの KV head を共有する 5 つの Q head を同一 WG で処理し、   KV を 1 回だけ読む。semantic K+V load が **42,434,560 B → 8,486,912 B**（5 分の 1）。 - **KV 分割**: tile 20 で 52 分割。WG 供給が 15.625% から大幅に改善。 - 数値差は `split_vs_direct` 最大絶対 **`1.08033e-7`**（F32 で約 1 ULP）に留まる。  **注意すべき教訓**: barrier pipeline は attention-only probe では 1.080422 倍改善したのに **full-model では 0.995427 倍と悪化**した。**probe 単体の倍率で採否を決めないこと。**  ## 本タスク: 同じ技法を prefill に当てる  依頼BK が prefill の M=1 tail 欠陥を解消した （コミット `17a531a2` / `c97ac7fa` / `bcf948b7`、 `benchmarks/results/2026-07-26/prefill-tail-fix/`）。4095 で 71.576 → 99.532 tok/s（1.391 倍）。  **しかし BK は残差の所在を明確に特定している:**  > tail 解消後の主残差は**長い causal prefix を持つ cached-prefix attention**であり、 > BH が作業中の attention kernel source は変更していません。  現状の prefill 実測（依頼AY、`benchmarks/results/2026-07-26/r9700-prefill-comparison/`、 BK 修正後の値）:  | prompt | uLLM | llama.cpp Q8_0 (F32 KV) | 差 | |---:|---:|---:|---:| | 128 | 865.278 | 1,165.756 | 1.347 倍 | | 512 | 519.872 | 1,195.722 | 2.300 倍 | | 1024 | 337.896 | 1,145.351 | 3.390 倍 | | 2048 | 189.685 | 1,058.379 | 5.580 倍 | | 4095 | 99.532 | 1,008.683 | **10.134 倍** |  **llama.cpp はほぼ平坦、uLLM だけが長さとともに崩落する。** prefill attention は O(N²) なので、attention カーネルが非効率なら**まさにこの形になる。**  **BH のファイルは解放された。** 対象カーネルは `runtime/src/ullm_runtime_parts/part_01.inc` と `runtime/src/ullm_runtime_hiprtc_sources.inc` にある:  - `ullm_cached_prefix_attn_f32_kernel` / `_flash2_kernel` - `ullm_cached_prefix_attn_fp8_e4m3_kernel` / `_flash2_kernel` / `_rocwmma_kernel` - `ullm_causal_attn_f32_kernel` / `_flash2_kernel` - `ullm_causal_attn_batch_f32_kernel` / `_flash2_kernel`  ## Phase 0: どれが本当に効いているかを測る  **decode で「generic matvec は本番で 1 回も起動していなかった」という事例がある。 prefill でも、まずどのカーネルが実際に時間を使っているかを測ること。**  1. **prefill の rocprof カーネル内訳を取ること**（プロンプト長を振って: 512 / 2048 / 4095）。    どのカーネルが何 % を占めるか。**長さによって内訳がどう変わるか。** 2. **起動ジオメトリと WG 数、wave 供給率**を、BH が decode で作った表と同じ形で出すこと。 3. **llama.cpp の prefill attention と並置比較すること。** 4. **崩落が本当に attention によるものかを確認すること。**    違うなら、そう報告すること。**私の想定に合わせないこと。**  ## Phase 1: 適用  Phase 0 の結果に従って設計すること。**decode の設計をそのまま貼らないこと。** prefill は M=N で decode と要求が違う:  - decode は 1 クエリ対 C 個の KV。prefill は N クエリ対 N（または prefix+N）個の KV。 - prefill は既に**タイル化された flash attention 系のカーネルがある**（`_flash2_`）。   **既存が何をしているかを読んでから足すこと。** - **GQA 協調が prefill でも成立するか**を確認すること   （decode では KV を 5 回読み直していた。prefill でも同じ構造か）。 - **BK の tail 修正（cursor rewind 方式）と衝突しないこと。**   BK は `crates/ullm-engine/src/sq8_serving_runtime.rs` を変更済みである。  ## Phase 2: 検証と計測  1. **数値**: BH と同じく、`1e-7` 規模（1 ULP）に収まることを確認すること。    **厳密な数値閾値を合否に使わないこと**（本プロジェクトでは閾値の誤用で    良い候補を落とす失敗が繰り返されている）。判定は    `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従うこと。 2. **速度は full-model の prefill tok/s で示すこと。**    **カーネル単体の倍率で採否を決めないこと**（BH の barrier pipeline の教訓）。    AY / BK と同一条件・同一会計で測ること    （`benchmarks/results/2026-07-26/r9700-prefill-comparison/conditions.md` と    `accounting.md`）。**128 / 512 / 1024 / 2048 / 4095 を測ること。** 3. **decode を壊していないことを確認すること。**    同じカーネルファイルを触るので、BH の 27.378731 tok/s が維持されることを確認すること。 4. **長さごとの改善率を出すこと。** 崩落カーブが平坦化したかが本質である。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。   `HIP_VISIBLE_DEVICES` で固定すること。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - 温度・クロック・throttle を記録し、クールダウンで開始温度を揃えること。   R9700 は junction crit 110 ℃、負荷時 97 ℃ 程度で最上位 DPM を維持する。   `amd-smi` の `THROTTLE_STATUS: THROTTLED` は具体的 violation が全て N/A であり、   それ自体はクロック低下の証拠ではない。**実クロックを見ること。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 4 本が並行して動いており、GPU 排他ロックと本番サービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）である。**   **GPU で計測する前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af 'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` は `StartLimitBurst=3` / `StartLimitIntervalSec=900`。   本日既に 2 回 start-limit に当たっている。**窓の回数を最小化すること。   停止したら必ず復旧すること。 - **依頼BM が `AQ4_0` P3 を本番に昇格させた直後である**   （新 manifest SHA-256 `a98910dc5bf59dc768e5bcd20bcf58968699540eb1b33df33066dcb6f274fe49`、   worker `/opt/ullm/aq4-p3-deployment-v0.1/releases/aq4-p3-c4c9a9b3/ullm-aq4-worker`）。   **`active.json` を変更しないこと。本タスクは昇格を行わない。** - 他セッション: **BJ**（`SQ8_0` 射影 / `SQ8_1` 候補再評価）、**BN**（MoE 量子化）、   **BP**（`SQ8_0` resident ランタイムの一般化。   `sq8_layer_runtime.rs` / `sq8_stack_runtime.rs` / `sq8_model_head_runtime.rs`）。   **これらのファイルには触らないこと。** - **`crates/ullm-engine/src/sq8_serving_runtime.rs` は BK が変更したばかりである。**   変更が必要なら、BK の tail 修正を壊さないことを確認し、理由を記録すること。  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` の本番実装コード（読むのは自由、変更は禁止） - `benchmarks/results/2026-07-26/` の既存ディレクトリ（**読み取りのみ**。自分の分は新規に作ること）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - **Phase 0 の内訳**（プロンプト長ごとに、どのカーネルが何 %、WG 数、wave 供給率、   llama.cpp との並置） - 実装と、**長さごとの full-model prefill tok/s**（128 / 512 / 1024 / 2048 / 4095） - **decode 27.378731 tok/s が維持されていることの確認** - 数値差の記録（1 ULP 規模に収まるか） - 結果を `benchmarks/results/2026-07-26/prefill-attention-redesign/` に保存 - `docs/plans/sq8-r9700-handwritten-kernel-optimization-plan-v0.1.md` に追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **prefill の時間内訳**（本当に attention が崩落の原因か。違うなら何か）、 (2) GQA 協調が prefill でも成立したか、 (3) **長さごとの prefill tok/s と、崩落カーブが平坦化したか**（llama.cpp との比較）、 (4) **decode 27.378731 tok/s は維持されたか**、 (5) 数値差は 1 ULP 規模に収まったか、 (6) GPU ロックとサービス窓の使用記録、`active.json` に触れていないこと、 (7) コミットハッシュ。
3314915 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'cat > /tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad/codex-BQ-ship-attention.md <<'"'"'PROMPT_EOF'"'"' # 依頼BQ: attention 再設計を本番に届ける（`AQ4_0` への適用可否 + 出荷配管）  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  依頼BH が decode attention を再設計し、**`SQ8_0` Qwen3-14B の full-model で 1.790050 倍**を達成した （コミット `15907d84` / `0455b119` / `b65e63c3` / `3d914439`、 `benchmarks/results/2026-07-26/sq8-decode-attention-redesign/`）。  - direct 15.228021 → generic tile 128 22.412990 → generic tile 20 23.872855   → **GQA grouped tile 20 27.378731 tok/s** - llama.cpp 30.468075 tok/s の **89.86%** - 数値差 `split_vs_direct` 最大絶対 **`1.08033e-7`**（F32 で約 1 ULP） - 効いた要素: **GQA 協調**（semantic K+V load 42,434,560 B → 8,486,912 B）と **KV 分割**（tile 20） - **教訓**: barrier pipeline は probe 単体で 1.080422 倍だが full-model で 0.995427 倍と悪化し、不採用。  **BH が挙げた唯一の出荷障害は、数値ではなく配管である:**  > 現行 served-model manifest は必須の `...SPLIT_TILE=20` を表現できず、 > この redesign を service candidate として起動して実文章の 10-prompt suite を実行できません。 > これは数値 gate の失敗ではなく、文章品質が未確認のためです。  一方、依頼BM が **`AQ4_0` P3 を本番に昇格させた** （コミット `bd42b349` / `2bac92be`、新 manifest SHA-256 `a98910dc5bf59dc768e5bcd20bcf58968699540eb1b33df33066dcb6f274fe49`、 worker `/opt/ullm/aq4-p3-deployment-v0.1/releases/aq4-p3-c4c9a9b3/ullm-aq4-worker`、 base commit `c4c9a9b344fc10e9a77ab0ded3293469d21b2f72`）。 本番実測は prefill **970.6107 tok/s**、decode C=1339 で **73.4568 tok/s**。  ## Phase 1: 最重要の問い — `AQ4_0` に適用できるか  **本番で動いているのは `AQ4_0` Qwen3.5-9B である。BH の 1.790 倍は `SQ8_0` Qwen3-14B の測定である。**  **この再設計が `AQ4_0` 経路にも適用できるなら、本番がそのまま速くなる。これを最初に判定すること。**  1. **`AQ4_0` Qwen3.5-9B の decode がどの attention カーネルを使っているかを、トレースで確認すること。**    `ullm_paged_decode_attn_f32_kernel` を共有しているのか、別系統か。    **コードを読んで推測するのではなく、rocprof のカーネルトレースで確認すること。**    （decode で「generic matvec は本番で 1 回も起動していなかった」という前例がある。） 2. **`AQ4_0` decode のカーネル時間内訳を出すこと。** attention が何 % か。    BH が `SQ8_0` で出した 51.05% に相当する数字。    **attention の比率が小さければ、再設計の効果は Amdahl で薄まる。** 3. **Qwen3.5-9B の attention 構造を確認すること。**    BM の記録によれば **32 layer、`linear_attention ×3 → full_attention` パターン**である。    - **linear attention 層には GQA 協調も KV 分割も適用できない可能性がある。**    - full attention 層が何層あり、そこが decode 時間の何 % かを出すこと。    - GQA 比（Q head 数 / KV head 数）を実 config から確認すること。      **GQA 比が 1 なら協調の利得はゼロである。** 4. **判定を出すこと。** 適用できるなら効果の見積もりを、    できないなら**なぜできないか**を書くこと。**期待に合わせないこと。**  **Phase 1 の結論次第で Phase 2 以降の価値が変わる。先に報告できる形にすること。**  ## Phase 2: 配管（manifest が実行設定を表現できるようにする）  BH の言う「manifest が `SPLIT_TILE=20` を表現できない」を解消すること。  1. 現行の served-model manifest スキーマ（`ullm.served_model.v2`）を読み、    **worker の実行設定をどこまで表現できるかを確認すること。** 2. **カーネル選択・タイル幅のような実行時パラメータを manifest で指定できるようにすること。**    - **fail-closed にすること。** 未知のキーや不正な値を黙って無視しないこと。    - **既存 manifest を壊さないこと。** 現行本番 `a98910dc…` がそのまま読めること。    - `tools/promote-served-model.py` / `rollback-promoted-served-model.py` が      新しいフィールドを正しく扱えること。**これらのツールの変更が必要なら、慎重に行うこと。** 3. **既存本番の回帰確認を行うこと。** 変更後も `AQ4_0` P3 worker が同じ挙動で動くこと。  ## Phase 3: `SQ8_0` 再設計の文章品質検証  **比較の土俵に注意すること。**  - 本番は `AQ4_0` Qwen3.5-9B、再設計候補は `SQ8_0` Qwen3-14B である。   **別モデル同士の文章品質比較は意味を持たない。** - **正しい対照は「同じ `SQ8_0` Qwen3-14B の direct カーネル版」である。**   direct 版と GQA-grouped tile 20 版を、同じ固定プロンプト集で並置すること。 - `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従うこと。   判定は**実際の生成文の品質**。**exact match や top-1 一致率を合否閾値にしないこと**   （観測値として記録するのは可）。  ## Phase 4: 昇格の判断  - **Phase 1 で `AQ4_0` に適用可能と判明した場合**: `AQ4_0` 版を実装・検証し、   **本番昇格まで行ってよい**（`tools/promote-served-model.py` を使うこと）。   これは同一モデルの高速化なので、本番の置き換えとして筋が通る。 - **`SQ8_0` Qwen3-14B を本番に昇格させないこと。**   `active.json` は単一モデルの manifest であり、昇格すると   本番が `AQ4_0` Qwen3.5-9B から別モデルに置き換わる。   **これは性能ではなく製品の判断であり、本タスクの範囲外である。**   `SQ8_0` 側は候補として検証し、証跡を残すに留めること。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - **カーネル単体の倍率で採否を決めないこと**（BH の barrier pipeline の教訓）。   **必ず full-model で示すこと。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 4 本が並行しており、GPU 排他ロックとサービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）。使う前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af '"'"'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'"'"'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` は現在 `inactive`**。これは依頼BJ が R9700 lock を保持しているための   **意図的な状態**である（BM が引き継ぎ記録に明記）。**勝手に起動して奪わないこと。** - `StartLimitBurst=3` / `StartLimitIntervalSec=900`。**本日 2 回 start-limit に当たっている。**   窓の回数を最小化すること。 - 他セッション: **BJ**（`SQ8_0` 射影 / `SQ8_1` 候補再評価、GPU 使用中）、   **BN**（MoE 量子化）、**BP**（`SQ8_0` resident ランタイム一般化。   `sq8_layer_runtime.rs` / `sq8_stack_runtime.rs` / `sq8_model_head_runtime.rs`）、   **BR**（prefill attention 再設計。   **`runtime/src/ullm_runtime_parts/part_01.inc` と   `runtime/src/ullm_runtime_hiprtc_sources.inc` を編集中**）。 - **BR が attention カーネルソースを編集中である。**   `AQ4_0` 向けにカーネル変更が必要になった場合、**BR と衝突しないこと。**   新規ファイルにするか、BR の完了を待つこと。**判断を記録すること。**  ## 触ってはいけないもの  - systemd ユニット内容、firewall / nftables 設定、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `benchmarks/results/2026-07-26/` の既存ディレクトリ（**読み取りのみ**。自分の分は新規に作ること）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。   ビルドが必要なら BM がやったように**別 worktree**を使うこと。  ## 成果物  - **Phase 1 の判定**（`AQ4_0` が同じ attention カーネルを使うか、   decode 時間の何 %、linear/full attention の構成、GQA 比、適用可否と効果見積もり） - manifest スキーマの拡張と、既存本番の回帰確認証跡 - `SQ8_0` direct 対 GQA-grouped の生成文並置証跡 - `AQ4_0` に適用した場合は、その実装と **full-model の本番速度**、昇格記録 - 結果を `benchmarks/results/2026-07-26/attention-redesign-shipping/` に保存 - `docs/plans/sq8-r9700-handwritten-kernel-optimization-plan-v0.1.md` に追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **`AQ4_0` 本番に BH の再設計は適用できるか**（トレース根拠つき。     できないならなぜか。できるなら decode 時間の何 % が対象で、見積もりは何倍か）、 (2) Qwen3.5-9B の linear/full attention 構成と GQA 比、 (3) manifest が実行設定を表現できるようになったか、既存本番は壊れていないか、 (4) `SQ8_0` direct 対 GQA-grouped の生成文品質はどうだったか、 (5) `AQ4_0` に適用した場合の **full-model 本番速度**と、昇格したか、 (6) GPU ロックとサービス窓の使用記録、現在の active manifest SHA-256、 (7) コミットハッシュ。 PROMPT_EOF S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad && codex exec -m gpt-5.6-terra -c model_reasoning_effort="max" -c service_tier="priority" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-BQ-ship-attention.md)" < /dev/null 2>&1 | tail -30' && pwd -P >| /tmp/claude-4aea-cwd
3314918 codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼BQ: attention 再設計を本番に届ける（`AQ4_0` への適用可否 + 出荷配管）  ## 背景  ユーザーの指示: **「各方面の改善を進められるだけ進めて。時間がなくて諦めるとかしなくていいから。」**  依頼BH が decode attention を再設計し、**`SQ8_0` Qwen3-14B の full-model で 1.790050 倍**を達成した （コミット `15907d84` / `0455b119` / `b65e63c3` / `3d914439`、 `benchmarks/results/2026-07-26/sq8-decode-attention-redesign/`）。  - direct 15.228021 → generic tile 128 22.412990 → generic tile 20 23.872855   → **GQA grouped tile 20 27.378731 tok/s** - llama.cpp 30.468075 tok/s の **89.86%** - 数値差 `split_vs_direct` 最大絶対 **`1.08033e-7`**（F32 で約 1 ULP） - 効いた要素: **GQA 協調**（semantic K+V load 42,434,560 B → 8,486,912 B）と **KV 分割**（tile 20） - **教訓**: barrier pipeline は probe 単体で 1.080422 倍だが full-model で 0.995427 倍と悪化し、不採用。  **BH が挙げた唯一の出荷障害は、数値ではなく配管である:**  > 現行 served-model manifest は必須の `...SPLIT_TILE=20` を表現できず、 > この redesign を service candidate として起動して実文章の 10-prompt suite を実行できません。 > これは数値 gate の失敗ではなく、文章品質が未確認のためです。  一方、依頼BM が **`AQ4_0` P3 を本番に昇格させた** （コミット `bd42b349` / `2bac92be`、新 manifest SHA-256 `a98910dc5bf59dc768e5bcd20bcf58968699540eb1b33df33066dcb6f274fe49`、 worker `/opt/ullm/aq4-p3-deployment-v0.1/releases/aq4-p3-c4c9a9b3/ullm-aq4-worker`、 base commit `c4c9a9b344fc10e9a77ab0ded3293469d21b2f72`）。 本番実測は prefill **970.6107 tok/s**、decode C=1339 で **73.4568 tok/s**。  ## Phase 1: 最重要の問い — `AQ4_0` に適用できるか  **本番で動いているのは `AQ4_0` Qwen3.5-9B である。BH の 1.790 倍は `SQ8_0` Qwen3-14B の測定である。**  **この再設計が `AQ4_0` 経路にも適用できるなら、本番がそのまま速くなる。これを最初に判定すること。**  1. **`AQ4_0` Qwen3.5-9B の decode がどの attention カーネルを使っているかを、トレースで確認すること。**    `ullm_paged_decode_attn_f32_kernel` を共有しているのか、別系統か。    **コードを読んで推測するのではなく、rocprof のカーネルトレースで確認すること。**    （decode で「generic matvec は本番で 1 回も起動していなかった」という前例がある。） 2. **`AQ4_0` decode のカーネル時間内訳を出すこと。** attention が何 % か。    BH が `SQ8_0` で出した 51.05% に相当する数字。    **attention の比率が小さければ、再設計の効果は Amdahl で薄まる。** 3. **Qwen3.5-9B の attention 構造を確認すること。**    BM の記録によれば **32 layer、`linear_attention ×3 → full_attention` パターン**である。    - **linear attention 層には GQA 協調も KV 分割も適用できない可能性がある。**    - full attention 層が何層あり、そこが decode 時間の何 % かを出すこと。    - GQA 比（Q head 数 / KV head 数）を実 config から確認すること。      **GQA 比が 1 なら協調の利得はゼロである。** 4. **判定を出すこと。** 適用できるなら効果の見積もりを、    できないなら**なぜできないか**を書くこと。**期待に合わせないこと。**  **Phase 1 の結論次第で Phase 2 以降の価値が変わる。先に報告できる形にすること。**  ## Phase 2: 配管（manifest が実行設定を表現できるようにする）  BH の言う「manifest が `SPLIT_TILE=20` を表現できない」を解消すること。  1. 現行の served-model manifest スキーマ（`ullm.served_model.v2`）を読み、    **worker の実行設定をどこまで表現できるかを確認すること。** 2. **カーネル選択・タイル幅のような実行時パラメータを manifest で指定できるようにすること。**    - **fail-closed にすること。** 未知のキーや不正な値を黙って無視しないこと。    - **既存 manifest を壊さないこと。** 現行本番 `a98910dc…` がそのまま読めること。    - `tools/promote-served-model.py` / `rollback-promoted-served-model.py` が      新しいフィールドを正しく扱えること。**これらのツールの変更が必要なら、慎重に行うこと。** 3. **既存本番の回帰確認を行うこと。** 変更後も `AQ4_0` P3 worker が同じ挙動で動くこと。  ## Phase 3: `SQ8_0` 再設計の文章品質検証  **比較の土俵に注意すること。**  - 本番は `AQ4_0` Qwen3.5-9B、再設計候補は `SQ8_0` Qwen3-14B である。   **別モデル同士の文章品質比較は意味を持たない。** - **正しい対照は「同じ `SQ8_0` Qwen3-14B の direct カーネル版」である。**   direct 版と GQA-grouped tile 20 版を、同じ固定プロンプト集で並置すること。 - `docs/plans/lightweight-promotion-policy-v0.1.md`（`ebf9a545`）に従うこと。   判定は**実際の生成文の品質**。**exact match や top-1 一致率を合否閾値にしないこと**   （観測値として記録するのは可）。  ## Phase 4: 昇格の判断  - **Phase 1 で `AQ4_0` に適用可能と判明した場合**: `AQ4_0` 版を実装・検証し、   **本番昇格まで行ってよい**（`tools/promote-served-model.py` を使うこと）。   これは同一モデルの高速化なので、本番の置き換えとして筋が通る。 - **`SQ8_0` Qwen3-14B を本番に昇格させないこと。**   `active.json` は単一モデルの manifest であり、昇格すると   本番が `AQ4_0` Qwen3.5-9B から別モデルに置き換わる。   **これは性能ではなく製品の判断であり、本タスクの範囲外である。**   `SQ8_0` 側は候補として検証し、証跡を残すに留めること。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **R9700 (gfx1201) のみ使用。V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。 - `llama-qwen35-udq4.service` は `inactive`/`disabled`。**起動しないこと。** - **プロファイラのレンジ時間を throughput として扱わないこと。** - **カーネル単体の倍率で採否を決めないこと**（BH の barrier pipeline の教訓）。   **必ず full-model で示すこと。** - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について（極めて重要）  **現在 4 本が並行しており、GPU 排他ロックとサービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）。使う前に必ず確認すること:**   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af 'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'   systemctl show ullm-openai.service -p ActiveState -p NRestarts   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` は現在 `inactive`**。これは依頼BJ が R9700 lock を保持しているための   **意図的な状態**である（BM が引き継ぎ記録に明記）。**勝手に起動して奪わないこと。** - `StartLimitBurst=3` / `StartLimitIntervalSec=900`。**本日 2 回 start-limit に当たっている。**   窓の回数を最小化すること。 - 他セッション: **BJ**（`SQ8_0` 射影 / `SQ8_1` 候補再評価、GPU 使用中）、   **BN**（MoE 量子化）、**BP**（`SQ8_0` resident ランタイム一般化。   `sq8_layer_runtime.rs` / `sq8_stack_runtime.rs` / `sq8_model_head_runtime.rs`）、   **BR**（prefill attention 再設計。   **`runtime/src/ullm_runtime_parts/part_01.inc` と   `runtime/src/ullm_runtime_hiprtc_sources.inc` を編集中**）。 - **BR が attention カーネルソースを編集中である。**   `AQ4_0` 向けにカーネル変更が必要になった場合、**BR と衝突しないこと。**   新規ファイルにするか、BR の完了を待つこと。**判断を記録すること。**  ## 触ってはいけないもの  - systemd ユニット内容、firewall / nftables 設定、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `benchmarks/results/2026-07-26/` の既存ディレクトリ（**読み取りのみ**。自分の分は新規に作ること）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。   ビルドが必要なら BM がやったように**別 worktree**を使うこと。  ## 成果物  - **Phase 1 の判定**（`AQ4_0` が同じ attention カーネルを使うか、   decode 時間の何 %、linear/full attention の構成、GQA 比、適用可否と効果見積もり） - manifest スキーマの拡張と、既存本番の回帰確認証跡 - `SQ8_0` direct 対 GQA-grouped の生成文並置証跡 - `AQ4_0` に適用した場合は、その実装と **full-model の本番速度**、昇格記録 - 結果を `benchmarks/results/2026-07-26/attention-redesign-shipping/` に保存 - `docs/plans/sq8-r9700-handwritten-kernel-optimization-plan-v0.1.md` に追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録 - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **`AQ4_0` 本番に BH の再設計は適用できるか**（トレース根拠つき。     できないならなぜか。できるなら decode 時間の何 % が対象で、見積もりは何倍か）、 (2) Qwen3.5-9B の linear/full attention 構成と GQA 比、 (3) manifest が実行設定を表現できるようになったか、既存本番は壊れていないか、 (4) `SQ8_0` direct 対 GQA-grouped の生成文品質はどうだったか、 (5) `AQ4_0` に適用した場合の **full-model 本番速度**と、昇格したか、 (6) GPU ロックとサービス窓の使用記録、現在の active manifest SHA-256、 (7) コミットハッシュ。
3331011 /bin/bash -c source /home/homelab1/.claude/shell-snapshots/snapshot-bash-1785030830118-1z2jq6.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && { \builtin unalias -- 'unsetenv'; \builtin unset -f -- 'unsetenv'; } >/dev/null 2>&1 || true && eval 'cat > /tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad/codex-BS-moe-quality.md <<'"'"'PROMPT_EOF'"'"' # 依頼BS: MoE `AQ4_0` package を正しい基準で判定し直す  ## この依頼は、前タスクの判定基準の誤りを正すものである  依頼BN が Qwen3.5-35B-A3B の `AQ4_0` package を製造した （コミット `433ae14e` / `de746861`、 `/home/homelab1/datapool/ullm/product/qwen35-35b-a3b-aq4_0-g8-moe-v0.2/`）。  製造結果は堅い:  - 方式 `aq4_e4m3_g8_ts_flloyd16`（実効 5 bpp）。router・shared expert・attention・   norm・embedding・`lm_head` は raw passthrough。 - package 全体 **23.310427 GiB**（routed experts 18.75 GiB）。R9700 (31.859 GiB) に収まる。 - 逆量子化検証は全 tensor 通過。relative MSE `0.003603673..0.004363885`、   max abs の外れ値は layer 39 `down_proj` の `0.043730080` 1 件のみ。 - codebook は routed `down_proj` / `gate_up_proj` ごとに 1 本を全 40×256 experts で共有。   per-expert / per-layer は held-out 比較で tail が悪化したため不採用（判断の根拠あり）。 - VRAM ledger: 262,144 token で `30,858,010,436 B`、headroom `3,350,732,988 B`。  **しかし package は `not_passed` と記録されている。その判定基準が誤っていた。**  前タスクの指示（私が書いた）は「expert 選択（top-k）が量子化前後で変わらないこと」を 必須にしていた。BN はこれを正しく実装し、正しく 2 つを区別して報告した:  - **router 単体**: 重み 40 枚すべて source と SHA-256 一致。   1,280 の条件付き入力で **top-k 変化ゼロ**。→ **router は正しい。** - **8 token × 40 layer の CPU streaming forward**: 量子化された expert 出力が下流に伝播し、   expert 集合が **105/320**、順序付き top-k が **238/320** 変化。   source-vs-source 対照は完全一致。  **後者は欠陥ではなく、量子化 MoE の正常な挙動である。** 上流の expert 出力が変われば下流の routing 入力も変わる。 256 experts / top-8 なら、上位 8 の境界付近にいる expert は僅かな摂動で日常的に入れ替わる。 入れ替わる expert 同士はスコアがほぼ拮抗しているので、出力への影響は小さいことが多い。  **判定すべきは expert 選択の安定性ではなく、生成された文章の品質である。** `docs/plans/lightweight-promotion-policy-v0.1.md`（コミット `ebf9a545`）がその基準である。  ## 本タスクの範囲  **MoE loader の結線を待たずに、既存の CPU streaming forward で品質を判定すること。**  依頼BI が残した未結線項目（loader 結線、hybrid attention、mRoPE、KV state、 Q output gate、weight residency）は本タスクの対象外である。 BN が作った `tools/validate-qwen35-moe-aq4-streaming-forward.py` を土台にすること。  ### Phase 1: 生成文による判定  1. **source（非量子化）と `AQ4_0` package の両方で、実際に文章を生成すること。**    CPU streaming forward で構わない。**遅くてよい。短くてよい。**    固定プロンプト集は `docs/plans/lightweight-promotion-prompt-suite-v0.1.json` を使うか、    CPU の速度に合わせて短縮版を作ること（**短縮したならそう明記すること**）。 2. **両者の出力を並置保存すること。** 人間が後から読める形にすること。 3. **判定は品質で行うこと:**    - 反復ループ、意味をなさない文字列、文字化け、言語の混線    - プロンプトへの応答の放棄    - 極端な長さの偏り    **greedy トークン列の完全一致を要求しないこと。** 4. **可能なら量的な指標も添えること**（perplexity、held-out の loss など、    CPU で現実的に取れる範囲で）。**ただし合否閾値にしないこと。**  ### Phase 2: package の再分類  1. Phase 1 の結果に基づき、package metadata の判定を更新すること。    **`not_passed` のままにするなら、生成文のどこがどう壊れていたかを具体的に示すこと。**    **通すなら、根拠となる生成文の証跡を指すこと。** 2. **expert 選択の変化率（105/320、238/320）は「観測値」として記録に残すこと。**    合否には使わないが、消さないこと。後で参照する価値がある。 3. **layer 39 `down_proj` の max abs 外れ値 `0.043730080`** が    生成文に影響しているかを確認できるなら確認すること。    できないなら「未確認」と明記すること。  ### Phase 3: もし品質が実際に崩れていた場合  **Phase 1 で本当に文章が壊れていた場合のみ**、改善策を検討すること:  - codebook の持ち方（現在は routed 投影ごとに 1 本を全 expert 共有） - layer 39 のような外れ値 tensor だけ raw passthrough にする - shared expert / 特定層の扱いを変える  **壊れていないなら、Phase 3 は行わないこと。** 不要な作業をしないこと。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo '"'"'Threadripper'"'"' | sudo -S -p '"'"''"'"' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **本タスクは CPU 中心である。GPU はほぼ不要なはずである。** - **CPU は空いている**（F32 参照コーパスは停止済み）。並列化してよい。   ただし他セッションのビルドを止めない程度に。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について  **現在 5 本が並行しており、GPU 排他ロックとサービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）。**   GPU を使う必要が生じたら、使う前に必ず確認すること:   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af '"'"'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'"'"'   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` を起動しないこと。** 本日 2 回 start-limit に当たっている。   本タスクはサービスを触る必要がない。 - **V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。 - 他セッション: **BJ**（`SQ8_0` 候補再評価、GPU 使用中）、   **BP**（`SQ8_0` resident ランタイム一般化。**MoE loader 結線に着手している可能性がある**）、   **BR**（prefill attention。`runtime/src/ullm_runtime_parts/part_01.inc` 等）、   **BQ**（attention 再設計の `AQ4_0` 適用、昇格を行う可能性あり）。 - **BP が MoE loader を触っている可能性がある。**   本タスクは loader 結線を行わないので衝突しないはずだが、   `crates/ullm-engine/` の MoE 関連ファイルを変更する必要が生じたら、   **衝突しないことを確認し、判断を記録すること。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` / `SQ8_0` の本番実装コード（読むのは自由、変更は禁止） - 既存の Qwen3.5-9B `AQ4_0` product / package（読み取りのみ）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - source 対 `AQ4_0` の**生成文の並置証跡**（人間が読める形で） - package metadata の判定更新（通すか、通さないかと、その具体的根拠） - expert 選択変化率を観測値として保持した記録 - `docs/plans/multi-architecture-support-plan-v0.1.md` および   `docs/plans/qwen35-moe-runtime-foundation-v0.1.md` に結果を追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録。   **判定基準を誤っていたため見直した、という経緯を明記すること。** - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **source と `AQ4_0` の生成文はどう違ったか**（具体的に）、 (2) 品質は崩れていたか、崩れていないか、 (3) package の判定をどう更新したか、 (4) layer 39 の外れ値は生成文に影響していたか、 (5) Phase 3 に進んだか（進んだなら何をしたか）、 (6) コミットハッシュ。 PROMPT_EOF S=/tmp/claude-1000/-home-homelab1-coding-local-ultimateLLM/dbf7a44e-519b-4d0b-b772-623467704df2/scratchpad && codex exec -m gpt-5.6-terra -c model_reasoning_effort="max" -c service_tier="priority" -c sandbox_mode="danger-full-access" -C /home/homelab1/coding-local/ultimateLLM/uLLM-project "$(cat $S/codex-BS-moe-quality.md)" < /dev/null 2>&1 | tail -30' && pwd -P >| /tmp/claude-7b65-cwd
3331014 codex exec -m gpt-5.6-terra -c model_reasoning_effort=max -c service_tier=priority -c sandbox_mode=danger-full-access -C /home/homelab1/coding-local/ultimateLLM/uLLM-project # 依頼BS: MoE `AQ4_0` package を正しい基準で判定し直す  ## この依頼は、前タスクの判定基準の誤りを正すものである  依頼BN が Qwen3.5-35B-A3B の `AQ4_0` package を製造した （コミット `433ae14e` / `de746861`、 `/home/homelab1/datapool/ullm/product/qwen35-35b-a3b-aq4_0-g8-moe-v0.2/`）。  製造結果は堅い:  - 方式 `aq4_e4m3_g8_ts_flloyd16`（実効 5 bpp）。router・shared expert・attention・   norm・embedding・`lm_head` は raw passthrough。 - package 全体 **23.310427 GiB**（routed experts 18.75 GiB）。R9700 (31.859 GiB) に収まる。 - 逆量子化検証は全 tensor 通過。relative MSE `0.003603673..0.004363885`、   max abs の外れ値は layer 39 `down_proj` の `0.043730080` 1 件のみ。 - codebook は routed `down_proj` / `gate_up_proj` ごとに 1 本を全 40×256 experts で共有。   per-expert / per-layer は held-out 比較で tail が悪化したため不採用（判断の根拠あり）。 - VRAM ledger: 262,144 token で `30,858,010,436 B`、headroom `3,350,732,988 B`。  **しかし package は `not_passed` と記録されている。その判定基準が誤っていた。**  前タスクの指示（私が書いた）は「expert 選択（top-k）が量子化前後で変わらないこと」を 必須にしていた。BN はこれを正しく実装し、正しく 2 つを区別して報告した:  - **router 単体**: 重み 40 枚すべて source と SHA-256 一致。   1,280 の条件付き入力で **top-k 変化ゼロ**。→ **router は正しい。** - **8 token × 40 layer の CPU streaming forward**: 量子化された expert 出力が下流に伝播し、   expert 集合が **105/320**、順序付き top-k が **238/320** 変化。   source-vs-source 対照は完全一致。  **後者は欠陥ではなく、量子化 MoE の正常な挙動である。** 上流の expert 出力が変われば下流の routing 入力も変わる。 256 experts / top-8 なら、上位 8 の境界付近にいる expert は僅かな摂動で日常的に入れ替わる。 入れ替わる expert 同士はスコアがほぼ拮抗しているので、出力への影響は小さいことが多い。  **判定すべきは expert 選択の安定性ではなく、生成された文章の品質である。** `docs/plans/lightweight-promotion-policy-v0.1.md`（コミット `ebf9a545`）がその基準である。  ## 本タスクの範囲  **MoE loader の結線を待たずに、既存の CPU streaming forward で品質を判定すること。**  依頼BI が残した未結線項目（loader 結線、hybrid attention、mRoPE、KV state、 Q output gate、weight residency）は本タスクの対象外である。 BN が作った `tools/validate-qwen35-moe-aq4-streaming-forward.py` を土台にすること。  ### Phase 1: 生成文による判定  1. **source（非量子化）と `AQ4_0` package の両方で、実際に文章を生成すること。**    CPU streaming forward で構わない。**遅くてよい。短くてよい。**    固定プロンプト集は `docs/plans/lightweight-promotion-prompt-suite-v0.1.json` を使うか、    CPU の速度に合わせて短縮版を作ること（**短縮したならそう明記すること**）。 2. **両者の出力を並置保存すること。** 人間が後から読める形にすること。 3. **判定は品質で行うこと:**    - 反復ループ、意味をなさない文字列、文字化け、言語の混線    - プロンプトへの応答の放棄    - 極端な長さの偏り    **greedy トークン列の完全一致を要求しないこと。** 4. **可能なら量的な指標も添えること**（perplexity、held-out の loss など、    CPU で現実的に取れる範囲で）。**ただし合否閾値にしないこと。**  ### Phase 2: package の再分類  1. Phase 1 の結果に基づき、package metadata の判定を更新すること。    **`not_passed` のままにするなら、生成文のどこがどう壊れていたかを具体的に示すこと。**    **通すなら、根拠となる生成文の証跡を指すこと。** 2. **expert 選択の変化率（105/320、238/320）は「観測値」として記録に残すこと。**    合否には使わないが、消さないこと。後で参照する価値がある。 3. **layer 39 `down_proj` の max abs 外れ値 `0.043730080`** が    生成文に影響しているかを確認できるなら確認すること。    できないなら「未確認」と明記すること。  ### Phase 3: もし品質が実際に崩れていた場合  **Phase 1 で本当に文章が壊れていた場合のみ**、改善策を検討すること:  - codebook の持ち方（現在は routed 投影ごとに 1 本を全 expert 共有） - layer 39 のような外れ値 tensor だけ raw passthrough にする - shared expert / 特定層の扱いを変える  **壊れていないなら、Phase 3 は行わないこと。** 不要な作業をしないこと。  ## 運用前提  - 開発専用マシン。sudo（`Threadripper`、非対話は `echo 'Threadripper' | sudo -S -p '' <cmd>`）は事前承認済み。 - **`git push` / rebase / amend / force は禁止。** - 命名規約: `AQ4` / `SQ8` はカテゴリ、`AQ4_0` / `SQ8_0` が厳密フォーマット名。常に厳密名で書くこと。 - **本タスクは CPU 中心である。GPU はほぼ不要なはずである。** - **CPU は空いている**（F32 参照コーパスは停止済み）。並列化してよい。   ただし他セッションのビルドを止めない程度に。 - **推測で埋めないこと。**確認できなかった点は「未確認」と明記すること。  ### 資源の共有について  **現在 5 本が並行しており、GPU 排他ロックとサービス再起動予算が律速である。**  - **R9700 の排他ロックは `/run/ullm/r9700.lock`（flock）。**   GPU を使う必要が生じたら、使う前に必ず確認すること:   ```   fuser -v /run/ullm/r9700.lock 2>&1   pgrep -af 'ullm-sq8-r9700|run_measurements.py|llama-bench|llama-server|promote-served-model'   ```   **保持されていれば待つこと。奪わないこと。** - **`ullm-openai.service` を起動しないこと。** 本日 2 回 start-limit に当たっている。   本タスクはサービスを触る必要がない。 - **V620 (gfx1030) は使用禁止**（パッシブ冷却、junction 100 ℃ 到達歴あり）。 - 他セッション: **BJ**（`SQ8_0` 候補再評価、GPU 使用中）、   **BP**（`SQ8_0` resident ランタイム一般化。**MoE loader 結線に着手している可能性がある**）、   **BR**（prefill attention。`runtime/src/ullm_runtime_parts/part_01.inc` 等）、   **BQ**（attention 再設計の `AQ4_0` 適用、昇格を行う可能性あり）。 - **BP が MoE loader を触っている可能性がある。**   本タスクは loader 結線を行わないので衝突しないはずだが、   `crates/ullm-engine/` の MoE 関連ファイルを変更する必要が生じたら、   **衝突しないことを確認し、判断を記録すること。**  ## 触ってはいけないもの  - `/etc/ullm/served-models/active.json`、systemd ユニット内容、`/opt/ullm` 配下（読み取りのみ可） - 上記の他セッションが編集中のファイル - `AQ4_0` / `SQ8_0` の本番実装コード（読むのは自由、変更は禁止） - 既存の Qwen3.5-9B `AQ4_0` product / package（読み取りのみ）  ## 並行実行に関する注意  - `git add` は自分が扱うファイルのパスを**明示的に列挙**して行う。`git add -A` / `git add .` は禁止。 - `git commit` が `index.lock` で失敗したら数秒待って再試行する。lock ファイルを削除しない。 - `git checkout` / `git reset` / `git stash` / `git clean` は実行しない。  ## 成果物  - source 対 `AQ4_0` の**生成文の並置証跡**（人間が読める形で） - package metadata の判定更新（通すか、通さないかと、その具体的根拠） - expert 選択変化率を観測値として保持した記録 - `docs/plans/multi-architecture-support-plan-v0.1.md` および   `docs/plans/qwen35-moe-runtime-foundation-v0.1.md` に結果を追記 - `journal/2026/07/26/` に house style（`## 前回の要点` / `## 今回の変更点` / `## 次の行動`）で記録。   **判定基準を誤っていたため見直した、という経緯を明記すること。** - 論理単位で分けたクリーンなコミット。`Co-Authored-By: GPT-5.6 Terra <noreply@openai.com>` を付けること。  ## 報告  (1) **source と `AQ4_0` の生成文はどう違ったか**（具体的に）、 (2) 品質は崩れていたか、崩れていないか、 (3) package の判定をどう更新したか、 (4) layer 39 の外れ値は生成文に影響していたか、 (5) Phase 3 に進んだか（進んだなら何をしたか）、 (6) コミットハッシュ。
